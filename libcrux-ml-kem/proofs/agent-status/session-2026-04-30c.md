# Session 2026-04-30c — `Ind_cpa.fst` un-laxed (per-fn cascade)

Followed agent prompt priority 1: dropped `Libcrux_ml_kem.Ind_cpa.fst`
from `ADMIT_MODULES`; cascaded `verification_status(lax)` to 16 of 21
function bodies; 3 fns now verify at PF/Hacspec end-to-end.

## Closed

- **Row 27 advanced ✅→⚠️ partial** — `Ind_cpa.fst` no longer module-
  admitted.  3 fns at PF/Hacspec via composition over per-fn lax
  callees: `serialize_public_key`, `serialize_public_key_mut`,
  `generate_keypair` (Q2 5.1 s, full hacspec ensures).  Module make
  passes 27 s wall (vs. >800 s cancelled previously).
- **AP-8 strengthened** in `proofs/agent-prompt-ml-kem.md` and memory
  (`feedback_rlimit_cap_800`) — user re-affirmed mid-session: never
  bump > 800; under split_queries cap is 400/query.  Prefer factoring,
  opacity, or lax over higher rlimit.

## Lax'd functions (16 of 21)

`serialize_vector`, `sample_ring_element_cbd`,
`sample_vector_cbd_then_ntt`, `generate_keypair_unpacked`,
`serialize_unpacked_secret_key`, `compress_then_serialize_u`,
`encrypt_c1`, `encrypt_c2`, `encrypt_unpacked`, `encrypt`,
`build_unpacked_public_key{,_mut}`, `deserialize_then_decompress_u`,
`deserialize_vector`, `decrypt_unpacked`, `decrypt`.

Each wall hit was the same shape: cascade of `Spec.MLKEM.*` post
through a multi-call body, Z3 incomplete-quantifiers / cancel.  Most
single-fn rlimits already at 500-800 (or 1500/2500 with split_queries
which violated cap); reduced 3 of those (sample_*, compress_*) to
within cap during this pass.

## Spec.MLKEM hit count delta

Net: 0 net change in citations — priority 1 was time-boxed and didn't
free time for migration.  However, **the priority-2 foundation
shipped (commit `a65ab3e43`)**:
`specs/ml-kem/proofs/fstar/commute/Hacspec_ml_kem.Parameters.Sizes.fst`
defines rank-parameterized versions of the top-5 Spec.MLKEM size
constants (`v_ETA1`, `v_ETA1_RANDOMNESS_SIZE`,
`v_RANKED_BYTES_PER_RING_ELEMENT`, `v_CPA_PUBLIC_KEY_SIZE`,
`v_CCA_PRIVATE_KEY_SIZE`) plus equality lemmas to Spec.MLKEM.  Module
makes pass in 1.16 s; all 16 queries succeed at rlimit 80.

This unblocks per-fn migration in `ind_cpa.rs` and `ind_cca.rs`:
consumers can now cite the `Hacspec_ml_kem.Parameters.Sizes.*` form
in ensures, and invoke the bundled `lemma_*_eq` to discharge any
remaining `Spec.MLKEM.*`-shaped obligation.

## Perf delta vs `fstar-perf-top20.md`

`Libcrux_ml_kem.Ind_cpa.fst`: was timing out > 800 s; now 11 s F\*
self-time, 27 s total.  Drops out of top-20.

## Final commit SHA

`504c881bb` — agent-mlkem: drop Ind_cpa.fst from ADMIT_MODULES via per-fn lax cascade.

## Next-priority recommendation

1. **`Spec.MLKEM` removal pass — per-fn migration in `src/ind_cpa.rs`
   and `src/ind_cca.rs`** (priority 2 continuation; foundation already
   shipped this session via `a65ab3e43`).  292 Rust hits across the
   two files.  Strategy: per `feedback_avoid_spec_mlkem`, replace
   `Spec.MLKEM.<C>` citations with
   `Hacspec_ml_kem.Parameters.Sizes.<C>` and use
   `lemma_<C>_eq` where the consumer needs the old shape.  One
   commit per function or small group; trace transitive cites
   (e.g. `Libcrux_ml_kem.Vector.to_spec_vector_t` may itself hit
   Spec.MLKEM internally — one hop down before committing).
2. **Un-lax cascade follow-up on `Ind_cpa.fst`** — start with
   `serialize_vector` body proof (the original blocker).  Path-of-
   least-resistance: factor `lemma_aux` to module scope so the per-
   iteration `eq_intro` instantiates a single fact.  Estimated 1-2
   sessions per cluster (3 clusters: serialize/sample, encrypt/build,
   deserialize/decrypt).
3. **Forward NTT layer 7 bridge** (gated, multi-session) — design
   choice still open.
