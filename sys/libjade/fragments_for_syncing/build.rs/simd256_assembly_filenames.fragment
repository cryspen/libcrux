{% for algorithm_type, algorithms in instructions.items() -%}
    {%- for algorithm in algorithms -%}
        {%- for platform, implementations in algorithm['platforms'].items() -%}
            {%- for implementation in implementations if implementation == 'avx2' %}
            "{{ algorithm['name'].replace('-', '_') }}_{{ implementation }}.s",
            {%- endfor -%}
        {%- endfor %}
    {%- endfor %}
{%- endfor %}

