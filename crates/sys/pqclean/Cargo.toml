[package]
name = "libcrux-pqclean"
version = "0.0.4"
edition = "2021"
authors = ["CE Labs"]
license = "Apache-2.0"
homepage = "https://github.com/celabshq/libcrux"
repository = "https://github.com/celabshq/libcrux"
readme = "Readme.md"
description = "pqclean wrapper for libcrux tests and benchmarks."
publish = false

[dependencies]

[build-dependencies]
cc = { version = "1.1", features = ["parallel"] }
bindgen = "0.72"
fs_extra = "1.2"
