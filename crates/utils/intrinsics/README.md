# SIMD Intrinsics

This crate provides `libcrux`-internal abstractions of selected SIMD instructions on AVX2 and Aarch64 platforms.

## `no_std` support

This crate supports `no_std` targets and is free of heap allocations.
