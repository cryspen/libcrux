name: ML-DSA - hax

on:
  merge_group:
  push:
    branches: ["dev", "main"]

  pull_request:
    branches: ["dev", "main"]

  schedule:
    - cron: "0 0 * * *"

  workflow_dispatch:
    inputs:
      hax_ref:
        description: "The hax revision you want this job to use"
        required: false
        type: string
      ssh_debug:
        description: "Enable SSH debug session with upterm"
        required: false
        type: boolean
        default: false

env:
  CARGO_TERM_COLOR: always

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # NOTE: This always runs, even if the `hax_ref` workflow input is provided.
  # TODO: Move this reusable workflow to a standalone action
  get-hax-ref:
    uses: ./.github/workflows/get-hax-ref.yml

  extract:
    if: ${{ github.event_name != 'merge_group' }}
    runs-on: ubuntu-latest
    needs:
      - get-hax-ref

    steps:
      - uses: actions/checkout@v5
      - uses: hacspec/hax-actions@main
        with:
          hax_reference: ${{ github.event.inputs.hax_ref || needs.get-hax-ref.outputs.hax_ref }}
          fstar: v2025.03.25

      - name: 🏃 Extract ML-DSA crate
        working-directory: libcrux-ml-dsa
        run: ./hax.sh extract

      - name: ↑ Upload F* extraction
        uses: actions/upload-artifact@v4
        with:
          name: fstar-extractions
          path: "**/proofs/fstar"
          include-hidden-files: true
          if-no-files-found: error

  lax:
    runs-on: ubuntu-latest
    needs:
      - get-hax-ref
      - extract
    if: ${{ github.event_name != 'merge_group' }}
    steps:
      - uses: actions/checkout@v5
      - uses: hacspec/hax-actions@main
        with:
          hax_reference: ${{ github.event.inputs.hax_ref || needs.get-hax-ref.outputs.hax_ref }}
          fstar: v2025.03.25

      - uses: actions/download-artifact@v5
        with:
          name: fstar-extractions
          path: .

      - name: 🏃 Lax ML-DSA crate
        working-directory: libcrux-ml-dsa
        run: ./hax.sh prove --admit
  
  prove:
    runs-on: self-hosted
    if: ${{ github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' }}
    needs:
      - get-hax-ref
      - extract
    steps:
      - uses: actions/checkout@v5
      - uses: actions/checkout@v5
        with:
          repository: cryspen/hax
          path: hax
          ref: ${{ github.event.inputs.hax_ref || needs.get-hax-ref.outputs.hax_ref }}
      - name: ⤵ Install hax
        run: |
          nix profile install ./hax
      - name: ⤵ Install FStar
        run: nix profile install github:FStarLang/FStar/v2025.02.17

      - uses: actions/download-artifact@v5
        with:
          name: fstar-extractions
          path: .

      - name: Setup upterm session
        uses: w95psp/action-upterm@main
        if: ${{ inputs.ssh_debug }}
        with:
          limit-access-to-actor: true
          limit-access-to-users: clementblaudeau,franziskuskiefer,jschneider-bensch,karthikbhargavan,keks,maximebuyse,W95Psp,wysiwys
          wait-timeout-minutes: 30

      - name: 🏃 Prove ML-DSA crate
        working-directory: libcrux-ml-dsa
        run: ./hax.sh prove

      - name: Setup upterm session
        uses: w95psp/action-upterm@main
        if: ${{ failure() }}
        with:
          limit-access-to-actor: true
          limit-access-to-users: clementblaudeau,franziskuskiefer,jschneider-bensch,karthikbhargavan,keks,maximebuyse,W95Psp,wysiwys
          wait-timeout-minutes: 5
        
  mldsa-extract-hax-status:
    if: ${{ always() }}
    needs: [get-hax-ref, extract]
    runs-on: ubuntu-latest
    steps:
      - name: Successful
        if: ${{ !(contains(needs.*.result, 'failure')) }} 
        run: exit 0 
      - name: Failing
        if: ${{ (contains(needs.*.result, 'failure')) }} 
        run: exit 1 

  mldsa-lax-hax-status:
    if: ${{ always() }}
    needs: [get-hax-ref, lax]
    runs-on: ubuntu-latest
    steps:
      - name: Successful
        if: ${{ !(contains(needs.*.result, 'failure')) }} 
        run: exit 0 
      - name: Failing
        if: ${{ (contains(needs.*.result, 'failure')) }} 
        run: exit 1 
