*fst
*fsti
# Hand-written companion theory relocated from src/arithmetic.rs fstar::before
# blocks (annotation-compaction campaign). Track it so CI can resolve the module
# (blanket *fst above would otherwise swallow it -> Error 72). See sha3 precedent.
!Libcrux_ml_dsa.Arithmetic_theory.fst
# Hand-written companion theory relocated from src/simd/portable/invntt.rs
# fstar::before blocks (annotation-compaction campaign). Track it (Error-72 trap).
!Libcrux_ml_dsa.Simd.Portable.Invntt_theory.fst
# Hand-written companion theory relocated from src/simd/portable/ntt.rs
# fstar::before blocks (annotation-compaction campaign). Track it (Error-72 trap).
!Libcrux_ml_dsa.Simd.Portable.Ntt_theory.fst
# Hand-written companion theory relocated from src/simd/avx2/ntt.rs
# fstar::before blocks (annotation-compaction campaign). Track it (Error-72 trap).
!Libcrux_ml_dsa.Simd.Avx2.Ntt_theory.fst
# Hand-written companion theory relocated from src/simd/avx2/invntt.rs
# fstar::before blocks (annotation-compaction campaign). Track it (Error-72 trap).
!Libcrux_ml_dsa.Simd.Avx2.Invntt_theory.fst
# Promoted from src/simd/avx2/rejection_sample/proof_helpers.rs, a #[cfg(hax)]-only
# module with no implementation whose 1183 lines were 1170 lines of fstar::before
# theory. The .rs shell is deleted, so this file is the source of truth rather than
# a generated artifact; hax no longer emits it (Error-72 trap still applies).
!Libcrux_ml_dsa.Simd.Avx2.Rejection_sample.Proof_helpers.fst
# Promoted from src/proof_utils.rs, likewise a #[cfg(hax)]-only marker-only module:
# 4 trusted-base assume-val lemmas (count_ones / movemask, no F* model in-tree).
# The .rs shell is deleted, so this file is the source of truth (Error-72 trap).
!Libcrux_ml_dsa.Proof_utils.fst
