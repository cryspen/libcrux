-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- [hacspec_ml_kem]: specs (statements of correctness)
import Aeneas
import CoreModels
import Hax
import HacspecMlKem.Extraction.Types
import HacspecMlKem.Extraction.Funs
open CoreModels Aeneas
open Aeneas.Std hiding namespace core alloc
open RustM ControlFlow Error
open Std.Do
set_option linter.dupNamespace false
set_option linter.hashCommand false
set_option linter.unusedVariables false
set_option linter.style.whitespace false
set_option linter.style.setOption false
set_option linter.style.longLine false

/- You can set the `maxHeartbeats` value with the `-max-heartbeats` CLI option -/
set_option maxHeartbeats 1000000

/- You can set the `maxRecDepth` value with the `-max-recdepth` CLI option -/
set_option maxRecDepth 2048

/- You can remove the following line by using the CLI option `-all-computable`: -/
noncomputable section

namespace hacspec_ml_kem


/-- [hacspec_ml_kem::parameters::cpa_ciphertext_size::pre]:
    Source: 'ml-kem/src/parameters.rs', lines 119:0-119:57 -/
@[reducible]
def parameters.cpa_ciphertext_size.pre (rank : Std.Usize) : RustM Bool := do
  if rank = 2#usize
  then ok true
  else if rank = 3#usize
       then ok true
       else ok (rank = 4#usize)

def parameters.cpa_ciphertext_size.spec (rank : Std.Usize) : Prop :=
  (parameters.cpa_ciphertext_size.pre rank).holds →
  ⦃ ⌜ True ⌝ ⦄
  parameters.cpa_ciphertext_size rank
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::parameters::rank_to_params::pre]:
    Source: 'ml-kem/src/parameters.rs', lines 136:0-136:57 -/
@[reducible]
def parameters.rank_to_params.pre (rank : Std.Usize) : RustM Bool := do
  if rank = 2#usize
  then ok true
  else if rank = 3#usize
       then ok true
       else ok (rank = 4#usize)

def parameters.rank_to_params.spec (rank : Std.Usize) : Prop :=
  (parameters.rank_to_params.pre rank).holds →
  ⦃ ⌜ True ⌝ ⦄
  parameters.rank_to_params rank
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::parameters::t_as_ntt_encoded_size::pre]:
    Source: 'ml-kem/src/parameters.rs', lines 173:0-173:57 -/
@[reducible]
def parameters.t_as_ntt_encoded_size.pre (rank : Std.Usize) : RustM Bool := do
  if rank = 2#usize
  then ok true
  else if rank = 3#usize
       then ok true
       else ok (rank = 4#usize)

def parameters.t_as_ntt_encoded_size.spec (rank : Std.Usize) : Prop :=
  (parameters.t_as_ntt_encoded_size.pre rank).holds →
  ⦃ ⌜ True ⌝ ⦄
  parameters.t_as_ntt_encoded_size rank
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::parameters::ranked_bytes_per_ring_element::pre]:
    Source: 'ml-kem/src/parameters.rs', lines 180:0-180:57 -/
@[reducible]
def parameters.ranked_bytes_per_ring_element.pre
  (rank : Std.Usize) : RustM Bool := do
  if rank = 2#usize
  then ok true
  else if rank = 3#usize
       then ok true
       else ok (rank = 4#usize)

def parameters.ranked_bytes_per_ring_element.spec (rank : Std.Usize) : Prop :=
  (parameters.ranked_bytes_per_ring_element.pre rank).holds →
  ⦃ ⌜ True ⌝ ⦄
  parameters.ranked_bytes_per_ring_element rank
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::parameters::cpa_public_key_size::pre]:
    Source: 'ml-kem/src/parameters.rs', lines 186:0-186:57 -/
@[reducible]
def parameters.cpa_public_key_size.pre (rank : Std.Usize) : RustM Bool := do
  if rank = 2#usize
  then ok true
  else if rank = 3#usize
       then ok true
       else ok (rank = 4#usize)

def parameters.cpa_public_key_size.spec (rank : Std.Usize) : Prop :=
  (parameters.cpa_public_key_size.pre rank).holds →
  ⦃ ⌜ True ⌝ ⦄
  parameters.cpa_public_key_size rank
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::parameters::cpa_private_key_size::pre]:
    Source: 'ml-kem/src/parameters.rs', lines 192:0-192:57 -/
@[reducible]
def parameters.cpa_private_key_size.pre (rank : Std.Usize) : RustM Bool := do
  if rank = 2#usize
  then ok true
  else if rank = 3#usize
       then ok true
       else ok (rank = 4#usize)

def parameters.cpa_private_key_size.spec (rank : Std.Usize) : Prop :=
  (parameters.cpa_private_key_size.pre rank).holds →
  ⦃ ⌜ True ⌝ ⦄
  parameters.cpa_private_key_size rank
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::parameters::cca_private_key_size::pre]:
    Source: 'ml-kem/src/parameters.rs', lines 198:0-198:57 -/
@[reducible]
def parameters.cca_private_key_size.pre (rank : Std.Usize) : RustM Bool := do
  if rank = 2#usize
  then ok true
  else if rank = 3#usize
       then ok true
       else ok (rank = 4#usize)

def parameters.cca_private_key_size.spec (rank : Std.Usize) : Prop :=
  (parameters.cca_private_key_size.pre rank).holds →
  ⦃ ⌜ True ⌝ ⦄
  parameters.cca_private_key_size rank
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::parameters::vector_u_compression_factor::pre]:
    Source: 'ml-kem/src/parameters.rs', lines 204:0-204:57 -/
@[reducible]
def parameters.vector_u_compression_factor.pre
  (rank : Std.Usize) : RustM Bool := do
  if rank = 2#usize
  then ok true
  else if rank = 3#usize
       then ok true
       else ok (rank = 4#usize)

def parameters.vector_u_compression_factor.spec (rank : Std.Usize) : Prop :=
  (parameters.vector_u_compression_factor.pre rank).holds →
  ⦃ ⌜ True ⌝ ⦄
  parameters.vector_u_compression_factor rank
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::parameters::vector_v_compression_factor::pre]:
    Source: 'ml-kem/src/parameters.rs', lines 214:0-214:57 -/
@[reducible]
def parameters.vector_v_compression_factor.pre
  (rank : Std.Usize) : RustM Bool := do
  if rank = 2#usize
  then ok true
  else if rank = 3#usize
       then ok true
       else ok (rank = 4#usize)

def parameters.vector_v_compression_factor.spec (rank : Std.Usize) : Prop :=
  (parameters.vector_v_compression_factor.pre rank).holds →
  ⦃ ⌜ True ⌝ ⦄
  parameters.vector_v_compression_factor rank
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::parameters::c1_block_size::pre]:
    Source: 'ml-kem/src/parameters.rs', lines 224:0-224:57 -/
@[reducible]
def parameters.c1_block_size.pre (rank : Std.Usize) : RustM Bool := do
  if rank = 2#usize
  then ok true
  else if rank = 3#usize
       then ok true
       else ok (rank = 4#usize)

def parameters.c1_block_size.spec (rank : Std.Usize) : Prop :=
  (parameters.c1_block_size.pre rank).holds →
  ⦃ ⌜ True ⌝ ⦄
  parameters.c1_block_size rank
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::parameters::c1_size::pre]:
    Source: 'ml-kem/src/parameters.rs', lines 230:0-230:57 -/
@[reducible]
def parameters.c1_size.pre (rank : Std.Usize) : RustM Bool := do
  if rank = 2#usize
  then ok true
  else if rank = 3#usize
       then ok true
       else ok (rank = 4#usize)

def parameters.c1_size.spec (rank : Std.Usize) : Prop :=
  (parameters.c1_size.pre rank).holds →
  ⦃ ⌜ True ⌝ ⦄
  parameters.c1_size rank
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::parameters::c2_size::pre]:
    Source: 'ml-kem/src/parameters.rs', lines 236:0-236:57 -/
@[reducible]
def parameters.c2_size.pre (rank : Std.Usize) : RustM Bool := do
  if rank = 2#usize
  then ok true
  else if rank = 3#usize
       then ok true
       else ok (rank = 4#usize)

def parameters.c2_size.spec (rank : Std.Usize) : Prop :=
  (parameters.c2_size.pre rank).holds →
  ⦃ ⌜ True ⌝ ⦄
  parameters.c2_size rank
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::parameters::eta1::pre]:
    Source: 'ml-kem/src/parameters.rs', lines 242:0-242:57 -/
@[reducible]
def parameters.eta1.pre (rank : Std.Usize) : RustM Bool := do
  if rank = 2#usize
  then ok true
  else if rank = 3#usize
       then ok true
       else ok (rank = 4#usize)

def parameters.eta1.spec (rank : Std.Usize) : Prop :=
  (parameters.eta1.pre rank).holds →
  ⦃ ⌜ True ⌝ ⦄
  parameters.eta1 rank
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::parameters::eta2::pre]:
    Source: 'ml-kem/src/parameters.rs', lines 252:0-252:57 -/
@[reducible]
def parameters.eta2.pre (rank : Std.Usize) : RustM Bool := do
  if rank = 2#usize
  then ok true
  else if rank = 3#usize
       then ok true
       else ok (rank = 4#usize)

def parameters.eta2.spec (rank : Std.Usize) : Prop :=
  (parameters.eta2.pre rank).holds →
  ⦃ ⌜ True ⌝ ⦄
  parameters.eta2 rank
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::parameters::eta1_randomness_size::pre]:
    Source: 'ml-kem/src/parameters.rs', lines 259:0-259:57 -/
@[reducible]
def parameters.eta1_randomness_size.pre (rank : Std.Usize) : RustM Bool := do
  if rank = 2#usize
  then ok true
  else if rank = 3#usize
       then ok true
       else ok (rank = 4#usize)

def parameters.eta1_randomness_size.spec (rank : Std.Usize) : Prop :=
  (parameters.eta1_randomness_size.pre rank).holds →
  ⦃ ⌜ True ⌝ ⦄
  parameters.eta1_randomness_size rank
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::parameters::eta2_randomness_size::pre]:
    Source: 'ml-kem/src/parameters.rs', lines 265:0-265:57 -/
@[reducible]
def parameters.eta2_randomness_size.pre (rank : Std.Usize) : RustM Bool := do
  if rank = 2#usize
  then ok true
  else if rank = 3#usize
       then ok true
       else ok (rank = 4#usize)

def parameters.eta2_randomness_size.spec (rank : Std.Usize) : Prop :=
  (parameters.eta2_randomness_size.pre rank).holds →
  ⦃ ⌜ True ⌝ ⦄
  parameters.eta2_randomness_size rank
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::parameters::implicit_rejection_hash_input_size::pre]:
    Source: 'ml-kem/src/parameters.rs', lines 271:0-271:57 -/
@[reducible]
def parameters.implicit_rejection_hash_input_size.pre
  (rank : Std.Usize) : RustM Bool := do
  if rank = 2#usize
  then ok true
  else if rank = 3#usize
       then ok true
       else ok (rank = 4#usize)

def parameters.implicit_rejection_hash_input_size.spec (rank : Std.Usize)
  : Prop :=
  (parameters.implicit_rejection_hash_input_size.pre rank).holds →
  ⦃ ⌜ True ⌝ ⦄
  parameters.implicit_rejection_hash_input_size rank
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::compress::compress::pre]:
    Source: 'ml-kem/src/compress.rs', lines 11:0-11:58 -/
@[reducible]
def compress.compress.pre
  (re : Array parameters.FieldElement 256#usize)
  (bits_per_compressed_coefficient : Std.Usize) :
  RustM Bool
  := do
  ok (bits_per_compressed_coefficient < 12#usize)

def compress.compress.spec (re : Array parameters.FieldElement 256#usize)
  (bits_per_compressed_coefficient : Std.Usize) : Prop :=
  (compress.compress.pre re bits_per_compressed_coefficient).holds →
  ⦃ ⌜ True ⌝ ⦄
  compress.compress re bits_per_compressed_coefficient
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::compress::decompress::pre]:
    Source: 'ml-kem/src/compress.rs', lines 23:0-25:96 -/
@[reducible]
def compress.decompress.pre
  (re : Array parameters.FieldElement 256#usize)
  (bits_per_compressed_coefficient : Std.Usize) :
  RustM hax_lib.prop.Prop
  := do
  let p ←
    hax_lib.Bool.Insts.Hax_libPropToProp.to_prop
      (bits_per_compressed_coefficient < 12#usize)
  let p1 ←
    hax_lib.prop.forall (core.convert.Into.Blanket (core.convert.From.Blanket
      hax_lib.prop.Prop))
      compress.__1.requires.closure.Insts.CoreOpsFunctionFnTupleUsizeProp (re,
      bits_per_compressed_coefficient)
  hax_lib.prop.Prop.Insts.CoreOpsBitBitAndTProp.bitand
    (core.convert.Into.Blanket (core.convert.From.Blanket hax_lib.prop.Prop)) p
    p1

def compress.decompress.spec (re : Array parameters.FieldElement 256#usize)
  (bits_per_compressed_coefficient : Std.Usize) : Prop :=
  (compress.decompress.pre re bits_per_compressed_coefficient).holds →
  ⦃ ⌜ True ⌝ ⦄
  compress.decompress re bits_per_compressed_coefficient
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::compress::compress_d::pre]:
    Source: 'ml-kem/src/compress.rs', lines 51:0-51:38 -/
@[reducible]
def compress.compress_d.pre
  (fe : parameters.FieldElement) (to_bit_size : Std.Usize) : RustM Bool := do
  ok (to_bit_size < 12#usize)

def compress.compress_d.spec (fe : parameters.FieldElement)
  (to_bit_size : Std.Usize) : Prop :=
  (compress.compress_d.pre fe to_bit_size).holds →
  ⦃ ⌜ True ⌝ ⦄
  compress.compress_d fe to_bit_size
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::compress::decompress_d::pre]:
    Source: 'ml-kem/src/compress.rs', lines 83:0-83:72 -/
@[reducible]
def compress.decompress_d.pre
  (fe : parameters.FieldElement) (to_bit_size : Std.Usize) : RustM Bool := do
  if to_bit_size < 12#usize
  then let i ← 1#u16 <<< to_bit_size
       ok (fe.val < i)
  else ok false

def compress.decompress_d.spec (fe : parameters.FieldElement)
  (to_bit_size : Std.Usize) : Prop :=
  (compress.decompress_d.pre fe to_bit_size).holds →
  ⦃ ⌜ True ⌝ ⦄
  compress.decompress_d fe to_bit_size
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ind_cca::keygen_internal::pre]:
    Source: 'ml-kem/src/ind_cca.rs', lines 20:0-26:2 -/
@[reducible]
def ind_cca.keygen_internal.pre
  (RANK : Std.Usize) (EK_SIZE : Std.Usize) (DK_PKE_SIZE : Std.Usize) (DK_SIZE :
  Std.Usize) (params : parameters.MlKemParams) (d : Array Std.U8 32#usize)
  (z : Array Std.U8 32#usize) :
  RustM Bool
  := do
  if RANK <= 4#usize
  then
    if params.rank = RANK
    then
      let i ← parameters.BYTES_PER_RING_ELEMENT
      let i1 ← RANK * i
      let i2 ← i1 + 32#usize
      if EK_SIZE = i2
      then
        let i3 ← RANK * i
        if DK_PKE_SIZE = i3
        then
          let i4 ← DK_PKE_SIZE + EK_SIZE
          let i5 ← i4 + parameters.hash_functions.H_DIGEST_SIZE
          let i6 ← i5 + 32#usize
          if DK_SIZE = i6
          then
            if params.eta1 = 2#usize
            then ok true
            else ok (params.eta1 = 3#usize)
          else ok false
        else ok false
      else ok false
    else ok false
  else ok false

def
  ind_cca.keygen_internal.spec (RANK : Std.Usize) (EK_SIZE : Std.Usize)
                              (DK_PKE_SIZE : Std.Usize) (DK_SIZE : Std.Usize)
  (params : parameters.MlKemParams) (d : Array Std.U8 32#usize)
  (z : Array Std.U8 32#usize) : Prop :=
  (ind_cca.keygen_internal.pre RANK EK_SIZE DK_PKE_SIZE DK_SIZE params d
  z).holds →
  ⦃ ⌜ True ⌝ ⦄
  ind_cca.keygen_internal RANK EK_SIZE DK_PKE_SIZE DK_SIZE params d z
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ind_cca::encaps_internal::pre]:
    Source: 'ml-kem/src/ind_cca.rs', lines 67:0-75:2 -/
@[reducible]
def ind_cca.encaps_internal.pre
  (RANK : Std.Usize) (U_SIZE : Std.Usize) (V_SIZE : Std.Usize) (CT_SIZE :
  Std.Usize) (params : parameters.MlKemParams) (ek : Slice Std.U8)
  (m : Array Std.U8 32#usize) :
  RustM Bool
  := do
  if RANK <= 4#usize
  then
    if params.rank = RANK
    then
      let i ← RANK * parameters.COEFFICIENTS_IN_RING_ELEMENT
      let i1 ← i * params.du
      let i2 ← i1 / 8#usize
      if U_SIZE = i2
      then
        let i3 ← parameters.COEFFICIENTS_IN_RING_ELEMENT * params.dv
        let i4 ← i3 / 8#usize
        if V_SIZE = i4
        then
          let i5 ← U_SIZE + V_SIZE
          if CT_SIZE = i5
          then
            let i6 ← core.slice.Slice.len ek
            let i7 ← parameters.BYTES_PER_RING_ELEMENT
            let i8 ← RANK * i7
            let i9 ← i8 + 32#usize
            if i6 = i9
            then
              if params.eta1 = 2#usize
              then
                if params.eta2 = 2#usize
                then ok true
                else ok (params.eta2 = 3#usize)
              else
                if params.eta1 = 3#usize
                then
                  if params.eta2 = 2#usize
                  then ok true
                  else ok (params.eta2 = 3#usize)
                else ok false
            else ok false
          else ok false
        else ok false
      else ok false
    else ok false
  else ok false

def
  ind_cca.encaps_internal.spec (RANK : Std.Usize) (U_SIZE : Std.Usize) (V_SIZE
                              : Std.Usize) (CT_SIZE : Std.Usize)
  (params : parameters.MlKemParams) (ek : Slice Std.U8)
  (m : Array Std.U8 32#usize) : Prop :=
  (ind_cca.encaps_internal.pre RANK U_SIZE V_SIZE CT_SIZE params ek m).holds
  →
  ⦃ ⌜ True ⌝ ⦄
  ind_cca.encaps_internal RANK U_SIZE V_SIZE CT_SIZE params ek m
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ind_cca::decaps_internal::pre]:
    Source: 'ml-kem/src/ind_cca.rs', lines 130:0-141:2 -/
@[reducible]
def ind_cca.decaps_internal.pre
  (RANK : Std.Usize) (EK_SIZE : Std.Usize) {DK_SIZE : Std.Usize} (DK_PKE_SIZE :
  Std.Usize) (U_SIZE : Std.Usize) (V_SIZE : Std.Usize) {CT_SIZE : Std.Usize}
  (J_INPUT_SIZE : Std.Usize) (params : parameters.MlKemParams)
  (dk : Array Std.U8 DK_SIZE) (c : Array Std.U8 CT_SIZE) :
  RustM Bool
  := do
  if RANK <= 4#usize
  then
    if params.rank = RANK
    then
      let i ← parameters.BYTES_PER_RING_ELEMENT
      let i1 ← RANK * i
      let i2 ← i1 + 32#usize
      if EK_SIZE = i2
      then
        let i3 ← RANK * i
        if DK_PKE_SIZE = i3
        then
          let i4 ← DK_PKE_SIZE + EK_SIZE
          let i5 ← i4 + parameters.hash_functions.H_DIGEST_SIZE
          let i6 ← i5 + 32#usize
          if DK_SIZE = i6
          then
            let i7 ← RANK * parameters.COEFFICIENTS_IN_RING_ELEMENT
            let i8 ← i7 * params.du
            let i9 ← i8 / 8#usize
            if U_SIZE = i9
            then
              let i10 ← parameters.COEFFICIENTS_IN_RING_ELEMENT * params.dv
              let i11 ← i10 / 8#usize
              if V_SIZE = i11
              then
                let i12 ← U_SIZE + V_SIZE
                if CT_SIZE = i12
                then
                  let i13 ← 32#usize + CT_SIZE
                  if J_INPUT_SIZE = i13
                  then
                    if params.eta1 = 2#usize
                    then
                      if params.eta2 = 2#usize
                      then ok true
                      else ok (params.eta2 = 3#usize)
                    else
                      if params.eta1 = 3#usize
                      then
                        if params.eta2 = 2#usize
                        then ok true
                        else ok (params.eta2 = 3#usize)
                      else ok false
                  else ok false
                else ok false
              else ok false
            else ok false
          else ok false
        else ok false
      else ok false
    else ok false
  else ok false

def
  ind_cca.decaps_internal.spec (RANK : Std.Usize) (EK_SIZE : Std.Usize)
                              {DK_SIZE : Std.Usize} (DK_PKE_SIZE : Std.Usize)
                              (U_SIZE : Std.Usize) (V_SIZE : Std.Usize)
                              {CT_SIZE : Std.Usize} (J_INPUT_SIZE : Std.Usize)
  (params : parameters.MlKemParams) (dk : Array Std.U8 DK_SIZE)
  (c : Array Std.U8 CT_SIZE) : Prop :=
  (ind_cca.decaps_internal.pre RANK EK_SIZE DK_PKE_SIZE U_SIZE V_SIZE
  J_INPUT_SIZE params dk c).holds →
  ⦃ ⌜ True ⌝ ⦄
  ind_cca.decaps_internal RANK EK_SIZE DK_PKE_SIZE U_SIZE V_SIZE J_INPUT_SIZE
  params dk c
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ind_cca::generate_keypair::pre]:
    Source: 'ml-kem/src/ind_cca.rs', lines 213:0-219:2 -/
@[reducible]
def ind_cca.generate_keypair.pre
  (RANK : Std.Usize) (EK_SIZE : Std.Usize) (DK_SIZE : Std.Usize) (DK_PKE_SIZE :
  Std.Usize) (params : parameters.MlKemParams)
  (randomness : Array Std.U8 64#usize) :
  RustM Bool
  := do
  if RANK <= 4#usize
  then
    if params.rank = RANK
    then
      let i ← parameters.BYTES_PER_RING_ELEMENT
      let i1 ← RANK * i
      let i2 ← i1 + 32#usize
      if EK_SIZE = i2
      then
        let i3 ← RANK * i
        if DK_PKE_SIZE = i3
        then
          let i4 ← DK_PKE_SIZE + EK_SIZE
          let i5 ← i4 + parameters.hash_functions.H_DIGEST_SIZE
          let i6 ← i5 + 32#usize
          if DK_SIZE = i6
          then
            if params.eta1 = 2#usize
            then ok true
            else ok (params.eta1 = 3#usize)
          else ok false
        else ok false
      else ok false
    else ok false
  else ok false

def
  ind_cca.generate_keypair.spec (RANK : Std.Usize) (EK_SIZE : Std.Usize)
                               (DK_SIZE : Std.Usize) (DK_PKE_SIZE : Std.Usize)
  (params : parameters.MlKemParams) (randomness : Array Std.U8 64#usize)
  : Prop :=
  (ind_cca.generate_keypair.pre RANK EK_SIZE DK_SIZE DK_PKE_SIZE params
  randomness).holds →
  ⦃ ⌜ True ⌝ ⦄
  ind_cca.generate_keypair RANK EK_SIZE DK_SIZE DK_PKE_SIZE params randomness
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ind_cca::public_key_modulus_check::pre]:
    Source: 'ml-kem/src/ind_cca.rs', lines 243:0-246:2 -/
@[reducible]
def ind_cca.public_key_modulus_check.pre
  {EK_SIZE : Std.Usize} (params : parameters.MlKemParams)
  (ek : Array Std.U8 EK_SIZE) :
  RustM Bool
  := do
  if params.rank <= 4#usize
  then
    let i ← parameters.BYTES_PER_RING_ELEMENT
    let i1 ← params.rank * i
    let i2 ← i1 + 32#usize
    ok (EK_SIZE = i2)
  else ok false

def ind_cca.public_key_modulus_check.spec {EK_SIZE : Std.Usize}
  (params : parameters.MlKemParams) (ek : Array Std.U8 EK_SIZE) : Prop :=
  (ind_cca.public_key_modulus_check.pre params ek).holds →
  ⦃ ⌜ True ⌝ ⦄
  ind_cca.public_key_modulus_check params ek
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ind_cca::encapsulate::pre]:
    Source: 'ml-kem/src/ind_cca.rs', lines 271:0-279:2 -/
@[reducible]
def ind_cca.encapsulate.pre
  (RANK : Std.Usize) {EK_SIZE : Std.Usize} (U_SIZE : Std.Usize) (V_SIZE :
  Std.Usize) (CT_SIZE : Std.Usize) (params : parameters.MlKemParams)
  (ek : Array Std.U8 EK_SIZE) (m : Array Std.U8 32#usize) :
  RustM Bool
  := do
  if RANK <= 4#usize
  then
    if params.rank = RANK
    then
      let i ← parameters.BYTES_PER_RING_ELEMENT
      let i1 ← RANK * i
      let i2 ← i1 + 32#usize
      if EK_SIZE = i2
      then
        let i3 ← RANK * parameters.COEFFICIENTS_IN_RING_ELEMENT
        let i4 ← i3 * params.du
        let i5 ← i4 / 8#usize
        if U_SIZE = i5
        then
          let i6 ← parameters.COEFFICIENTS_IN_RING_ELEMENT * params.dv
          let i7 ← i6 / 8#usize
          if V_SIZE = i7
          then
            let i8 ← U_SIZE + V_SIZE
            if CT_SIZE = i8
            then
              if params.eta1 = 2#usize
              then
                if params.eta2 = 2#usize
                then ok true
                else ok (params.eta2 = 3#usize)
              else
                if params.eta1 = 3#usize
                then
                  if params.eta2 = 2#usize
                  then ok true
                  else ok (params.eta2 = 3#usize)
                else ok false
            else ok false
          else ok false
        else ok false
      else ok false
    else ok false
  else ok false

def
  ind_cca.encapsulate.spec (RANK : Std.Usize) {EK_SIZE : Std.Usize} (U_SIZE :
                          Std.Usize) (V_SIZE : Std.Usize) (CT_SIZE : Std.Usize)
  (params : parameters.MlKemParams) (ek : Array Std.U8 EK_SIZE)
  (m : Array Std.U8 32#usize) : Prop :=
  (ind_cca.encapsulate.pre RANK U_SIZE V_SIZE CT_SIZE params ek m).holds →
  ⦃ ⌜ True ⌝ ⦄
  ind_cca.encapsulate RANK U_SIZE V_SIZE CT_SIZE params ek m
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ind_cca::decapsulate::pre]:
    Source: 'ml-kem/src/ind_cca.rs', lines 310:0-321:2 -/
@[reducible]
def ind_cca.decapsulate.pre
  (RANK : Std.Usize) (EK_SIZE : Std.Usize) {DK_SIZE : Std.Usize} (DK_PKE_SIZE :
  Std.Usize) (U_SIZE : Std.Usize) (V_SIZE : Std.Usize) {CT_SIZE : Std.Usize}
  (J_INPUT_SIZE : Std.Usize) (params : parameters.MlKemParams)
  (dk : Array Std.U8 DK_SIZE) (c : Array Std.U8 CT_SIZE) :
  RustM Bool
  := do
  if RANK <= 4#usize
  then
    if params.rank = RANK
    then
      let i ← parameters.BYTES_PER_RING_ELEMENT
      let i1 ← RANK * i
      let i2 ← i1 + 32#usize
      if EK_SIZE = i2
      then
        let i3 ← RANK * i
        if DK_PKE_SIZE = i3
        then
          let i4 ← DK_PKE_SIZE + EK_SIZE
          let i5 ← i4 + parameters.hash_functions.H_DIGEST_SIZE
          let i6 ← i5 + 32#usize
          if DK_SIZE = i6
          then
            let i7 ← RANK * parameters.COEFFICIENTS_IN_RING_ELEMENT
            let i8 ← i7 * params.du
            let i9 ← i8 / 8#usize
            if U_SIZE = i9
            then
              let i10 ← parameters.COEFFICIENTS_IN_RING_ELEMENT * params.dv
              let i11 ← i10 / 8#usize
              if V_SIZE = i11
              then
                let i12 ← U_SIZE + V_SIZE
                if CT_SIZE = i12
                then
                  let i13 ← 32#usize + CT_SIZE
                  if J_INPUT_SIZE = i13
                  then
                    if params.eta1 = 2#usize
                    then
                      if params.eta2 = 2#usize
                      then ok true
                      else ok (params.eta2 = 3#usize)
                    else
                      if params.eta1 = 3#usize
                      then
                        if params.eta2 = 2#usize
                        then ok true
                        else ok (params.eta2 = 3#usize)
                      else ok false
                  else ok false
                else ok false
              else ok false
            else ok false
          else ok false
        else ok false
      else ok false
    else ok false
  else ok false

def
  ind_cca.decapsulate.spec (RANK : Std.Usize) (EK_SIZE : Std.Usize) {DK_SIZE :
                          Std.Usize} (DK_PKE_SIZE : Std.Usize) (U_SIZE :
                          Std.Usize) (V_SIZE : Std.Usize) {CT_SIZE : Std.Usize}
                          (J_INPUT_SIZE : Std.Usize)
  (params : parameters.MlKemParams) (dk : Array Std.U8 DK_SIZE)
  (c : Array Std.U8 CT_SIZE) : Prop :=
  (ind_cca.decapsulate.pre RANK EK_SIZE DK_PKE_SIZE U_SIZE V_SIZE J_INPUT_SIZE
  params dk c).holds →
  ⦃ ⌜ True ⌝ ⦄
  ind_cca.decapsulate RANK EK_SIZE DK_PKE_SIZE U_SIZE V_SIZE J_INPUT_SIZE
  params dk c
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ind_cca::ind_cca_unpack_generate_keypair::pre]:
    Source: 'ml-kem/src/ind_cca.rs', lines 376:0-380:2 -/
@[reducible]
def ind_cca.ind_cca_unpack_generate_keypair.pre
  (RANK : Std.Usize) (EK_SIZE : Std.Usize) (params : parameters.MlKemParams)
  (randomness : Array Std.U8 64#usize) :
  RustM Bool
  := do
  if RANK <= 4#usize
  then
    if params.rank = RANK
    then
      let i ← parameters.BYTES_PER_RING_ELEMENT
      let i1 ← RANK * i
      let i2 ← i1 + 32#usize
      if EK_SIZE = i2
      then
        if params.eta1 = 2#usize
        then ok true
        else ok (params.eta1 = 3#usize)
      else ok false
    else ok false
  else ok false

def
  ind_cca.ind_cca_unpack_generate_keypair.spec (RANK : Std.Usize) (EK_SIZE :
                                              Std.Usize)
  (params : parameters.MlKemParams) (randomness : Array Std.U8 64#usize)
  : Prop :=
  (ind_cca.ind_cca_unpack_generate_keypair.pre RANK EK_SIZE params
  randomness).holds →
  ⦃ ⌜ True ⌝ ⦄
  ind_cca.ind_cca_unpack_generate_keypair RANK EK_SIZE params randomness
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ind_cca::ind_cca_unpack_encapsulate::pre]:
    Source: 'ml-kem/src/ind_cca.rs', lines 420:0-427:2 -/
@[reducible]
def ind_cca.ind_cca_unpack_encapsulate.pre
  {RANK : Std.Usize} (U_SIZE : Std.Usize) (V_SIZE : Std.Usize) (CT_SIZE :
  Std.Usize) (params : parameters.MlKemParams)
  (public_key_hash : Array Std.U8 32#usize)
  (t_as_ntt : Array (Array parameters.FieldElement 256#usize) RANK)
  (m_A : Array (Array (Array parameters.FieldElement 256#usize) RANK) RANK)
  (randomness : Array Std.U8 32#usize) :
  RustM Bool
  := do
  if RANK <= 4#usize
  then
    if params.rank = RANK
    then
      let i ← RANK * parameters.COEFFICIENTS_IN_RING_ELEMENT
      let i1 ← i * params.du
      let i2 ← i1 / 8#usize
      if U_SIZE = i2
      then
        let i3 ← parameters.COEFFICIENTS_IN_RING_ELEMENT * params.dv
        let i4 ← i3 / 8#usize
        if V_SIZE = i4
        then
          let i5 ← U_SIZE + V_SIZE
          if CT_SIZE = i5
          then
            if params.eta1 = 2#usize
            then
              if params.eta2 = 2#usize
              then ok true
              else ok (params.eta2 = 3#usize)
            else
              if params.eta1 = 3#usize
              then
                if params.eta2 = 2#usize
                then ok true
                else ok (params.eta2 = 3#usize)
              else ok false
          else ok false
        else ok false
      else ok false
    else ok false
  else ok false

def
  ind_cca.ind_cca_unpack_encapsulate.spec {RANK : Std.Usize} (U_SIZE :
                                         Std.Usize) (V_SIZE : Std.Usize)
                                         (CT_SIZE : Std.Usize)
  (params : parameters.MlKemParams) (public_key_hash : Array Std.U8 32#usize)
  (t_as_ntt : Array (Array parameters.FieldElement 256#usize) RANK)
  (m_A : Array (Array (Array parameters.FieldElement 256#usize) RANK) RANK)
  (randomness : Array Std.U8 32#usize) : Prop :=
  (ind_cca.ind_cca_unpack_encapsulate.pre U_SIZE V_SIZE CT_SIZE params
  public_key_hash t_as_ntt m_A randomness).holds →
  ⦃ ⌜ True ⌝ ⦄
  ind_cca.ind_cca_unpack_encapsulate U_SIZE V_SIZE CT_SIZE params
  public_key_hash t_as_ntt m_A randomness
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ind_cca::ind_cca_unpack_decapsulate::pre]:
    Source: 'ml-kem/src/ind_cca.rs', lines 469:0-477:2 -/
@[reducible]
def ind_cca.ind_cca_unpack_decapsulate.pre
  {RANK : Std.Usize} (U_SIZE : Std.Usize) (V_SIZE : Std.Usize) {CT_SIZE :
  Std.Usize} (J_INPUT_SIZE : Std.Usize) (params : parameters.MlKemParams)
  (public_key_hash : Array Std.U8 32#usize)
  (implicit_rejection_value : Array Std.U8 32#usize)
  (ciphertext : Array Std.U8 CT_SIZE)
  (secret_as_ntt : Array (Array parameters.FieldElement 256#usize) RANK)
  (t_as_ntt : Array (Array parameters.FieldElement 256#usize) RANK)
  (m_A : Array (Array (Array parameters.FieldElement 256#usize) RANK) RANK) :
  RustM Bool
  := do
  if RANK <= 4#usize
  then
    if params.rank = RANK
    then
      let i ← RANK * parameters.COEFFICIENTS_IN_RING_ELEMENT
      let i1 ← i * params.du
      let i2 ← i1 / 8#usize
      if U_SIZE = i2
      then
        let i3 ← parameters.COEFFICIENTS_IN_RING_ELEMENT * params.dv
        let i4 ← i3 / 8#usize
        if V_SIZE = i4
        then
          let i5 ← U_SIZE + V_SIZE
          if CT_SIZE = i5
          then
            let i6 ← 32#usize + CT_SIZE
            if J_INPUT_SIZE = i6
            then
              if params.eta1 = 2#usize
              then
                if params.eta2 = 2#usize
                then ok true
                else ok (params.eta2 = 3#usize)
              else
                if params.eta1 = 3#usize
                then
                  if params.eta2 = 2#usize
                  then ok true
                  else ok (params.eta2 = 3#usize)
                else ok false
            else ok false
          else ok false
        else ok false
      else ok false
    else ok false
  else ok false

def
  ind_cca.ind_cca_unpack_decapsulate.spec {RANK : Std.Usize} (U_SIZE :
                                         Std.Usize) (V_SIZE : Std.Usize)
                                         {CT_SIZE : Std.Usize} (J_INPUT_SIZE :
                                         Std.Usize)
  (params : parameters.MlKemParams) (public_key_hash : Array Std.U8 32#usize)
  (implicit_rejection_value : Array Std.U8 32#usize)
  (ciphertext : Array Std.U8 CT_SIZE)
  (secret_as_ntt : Array (Array parameters.FieldElement 256#usize) RANK)
  (t_as_ntt : Array (Array parameters.FieldElement 256#usize) RANK)
  (m_A : Array (Array (Array parameters.FieldElement 256#usize) RANK) RANK)
  : Prop :=
  (ind_cca.ind_cca_unpack_decapsulate.pre U_SIZE V_SIZE J_INPUT_SIZE params
  public_key_hash implicit_rejection_value ciphertext secret_as_ntt t_as_ntt
  m_A).holds →
  ⦃ ⌜ True ⌝ ⦄
  ind_cca.ind_cca_unpack_decapsulate U_SIZE V_SIZE J_INPUT_SIZE params
  public_key_hash implicit_rejection_value ciphertext secret_as_ntt t_as_ntt
  m_A
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ind_cpa::sample_secret::pre]:
    Source: 'ml-kem/src/ind_cpa.rs', lines 18:0-18:42 -/
@[reducible]
def ind_cpa.sample_secret.pre
  (eta : Std.Usize) (prf_input : Array Std.U8 33#usize) : RustM Bool := do
  if eta = 2#usize
  then ok true
  else ok (eta = 3#usize)

def ind_cpa.sample_secret.spec (eta : Std.Usize)
  (prf_input : Array Std.U8 33#usize) : Prop :=
  (ind_cpa.sample_secret.pre eta prf_input).holds →
  ⦃ ⌜ True ⌝ ⦄
  ind_cpa.sample_secret eta prf_input
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ind_cpa::concat_byte::pre]:
    Source: 'ml-kem/src/ind_cpa.rs', lines 33:0-33:43 -/
@[reducible]
def ind_cpa.concat_byte.pre
  {N : Std.Usize} (N1 : Std.Usize) (a : Array Std.U8 N) (b : Std.U8) :
  RustM Bool
  := do
  if N1 > 0#usize
  then let i ← N1 - 1#usize
       ok (N = i)
  else ok false

def ind_cpa.concat_byte.spec {N : Std.Usize} (N1 : Std.Usize)
  (a : Array Std.U8 N) (b : Std.U8) : Prop :=
  (ind_cpa.concat_byte.pre N1 a b).holds →
  ⦃ ⌜ True ⌝ ⦄
  ind_cpa.concat_byte N1 a b
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ind_cpa::sample_vector_cbd::pre]:
    Source: 'ml-kem/src/ind_cpa.rs', lines 47:0-52:2 -/
@[reducible]
def ind_cpa.sample_vector_cbd.pre
  (RANK : Std.Usize) (eta : Std.Usize) (seed : Slice Std.U8)
  (domain_separator : Std.U8) :
  RustM Bool
  := do
  let i ← core.slice.Slice.len seed
  if i = 32#usize
  then
    if eta = 2#usize
    then
      if RANK <= 4#usize
      then
        let i1 ← lift (UScalar.cast .Usize domain_separator)
        let i2 ← i1 + RANK
        ok (i2 < 256#usize)
      else ok false
    else
      if eta = 3#usize
      then
        if RANK <= 4#usize
        then
          let i1 ← lift (UScalar.cast .Usize domain_separator)
          let i2 ← i1 + RANK
          ok (i2 < 256#usize)
        else ok false
      else ok false
  else ok false

def ind_cpa.sample_vector_cbd.spec (RANK : Std.Usize) (eta : Std.Usize)
  (seed : Slice Std.U8) (domain_separator : Std.U8) : Prop :=
  (ind_cpa.sample_vector_cbd.pre RANK eta seed domain_separator).holds →
  ⦃ ⌜ True ⌝ ⦄
  ind_cpa.sample_vector_cbd RANK eta seed domain_separator
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ind_cpa::sample_vector_cbd_then_ntt::pre]:
    Source: 'ml-kem/src/ind_cpa.rs', lines 67:0-72:2 -/
@[reducible]
def ind_cpa.sample_vector_cbd_then_ntt.pre
  (RANK : Std.Usize) (eta : Std.Usize) (seed : Slice Std.U8)
  (domain_separator : Std.U8) :
  RustM Bool
  := do
  let i ← core.slice.Slice.len seed
  if i = 32#usize
  then
    if eta = 2#usize
    then
      if RANK <= 4#usize
      then
        let i1 ← lift (UScalar.cast .Usize domain_separator)
        let i2 ← i1 + RANK
        ok (i2 < 256#usize)
      else ok false
    else
      if eta = 3#usize
      then
        if RANK <= 4#usize
        then
          let i1 ← lift (UScalar.cast .Usize domain_separator)
          let i2 ← i1 + RANK
          ok (i2 < 256#usize)
        else ok false
      else ok false
  else ok false

def ind_cpa.sample_vector_cbd_then_ntt.spec (RANK : Std.Usize)
  (eta : Std.Usize) (seed : Slice Std.U8) (domain_separator : Std.U8) : Prop :=
  (ind_cpa.sample_vector_cbd_then_ntt.pre RANK eta seed domain_separator).holds
  →
  ⦃ ⌜ True ⌝ ⦄
  ind_cpa.sample_vector_cbd_then_ntt RANK eta seed domain_separator
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ind_cpa::generate_keypair_unpacked::pre]:
    Source: 'ml-kem/src/ind_cpa.rs', lines 97:0-101:2 -/
@[reducible]
def ind_cpa.generate_keypair_unpacked.pre
  (RANK : Std.Usize) (params : parameters.MlKemParams)
  (key_generation_seed : Slice Std.U8) :
  RustM Bool
  := do
  if RANK <= 4#usize
  then
    if params.rank = RANK
    then
      if params.eta1 = 2#usize
      then let i ← core.slice.Slice.len key_generation_seed
           ok (i = 32#usize)
      else
        if params.eta1 = 3#usize
        then
          let i ← core.slice.Slice.len key_generation_seed
          ok (i = 32#usize)
        else ok false
    else ok false
  else ok false

def ind_cpa.generate_keypair_unpacked.spec (RANK : Std.Usize)
  (params : parameters.MlKemParams) (key_generation_seed : Slice Std.U8)
  : Prop :=
  (ind_cpa.generate_keypair_unpacked.pre RANK params key_generation_seed).holds
  →
  ⦃ ⌜ True ⌝ ⦄
  ind_cpa.generate_keypair_unpacked RANK params key_generation_seed
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ind_cpa::generate_keypair::pre]:
    Source: 'ml-kem/src/ind_cpa.rs', lines 167:0-173:2 -/
@[reducible]
def ind_cpa.generate_keypair.pre
  (RANK : Std.Usize) (EK_SIZE : Std.Usize) (DK_PKE_SIZE : Std.Usize)
  (params : parameters.MlKemParams) (key_generation_seed : Slice Std.U8) :
  RustM Bool
  := do
  if RANK <= 4#usize
  then
    if params.rank = RANK
    then
      let i ← parameters.BYTES_PER_RING_ELEMENT
      let i1 ← RANK * i
      let i2 ← i1 + 32#usize
      if EK_SIZE = i2
      then
        let i3 ← RANK * i
        if DK_PKE_SIZE = i3
        then
          if params.eta1 = 2#usize
          then
            let i4 ← core.slice.Slice.len key_generation_seed
            ok (i4 = 32#usize)
          else
            if params.eta1 = 3#usize
            then
              let i4 ← core.slice.Slice.len key_generation_seed
              ok (i4 = 32#usize)
            else ok false
        else ok false
      else ok false
    else ok false
  else ok false

def
  ind_cpa.generate_keypair.spec (RANK : Std.Usize) (EK_SIZE : Std.Usize)
                               (DK_PKE_SIZE : Std.Usize)
  (params : parameters.MlKemParams) (key_generation_seed : Slice Std.U8)
  : Prop :=
  (ind_cpa.generate_keypair.pre RANK EK_SIZE DK_PKE_SIZE params
  key_generation_seed).holds →
  ⦃ ⌜ True ⌝ ⦄
  ind_cpa.generate_keypair RANK EK_SIZE DK_PKE_SIZE params key_generation_seed
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ind_cpa::encrypt::pre]:
    Source: 'ml-kem/src/ind_cpa.rs', lines 236:0-245:2 -/
@[reducible]
def ind_cpa.encrypt.pre
  (RANK : Std.Usize) (U_SIZE : Std.Usize) (V_SIZE : Std.Usize) (CT_SIZE :
  Std.Usize) (params : parameters.MlKemParams) (ek : Slice Std.U8)
  (message : Array Std.U8 32#usize) (randomness : Slice Std.U8) :
  RustM Bool
  := do
  if RANK <= 4#usize
  then
    if params.rank = RANK
    then
      let i ← RANK * parameters.COEFFICIENTS_IN_RING_ELEMENT
      let i1 ← i * params.du
      let i2 ← i1 / 8#usize
      if U_SIZE = i2
      then
        let i3 ← parameters.COEFFICIENTS_IN_RING_ELEMENT * params.dv
        let i4 ← i3 / 8#usize
        if V_SIZE = i4
        then
          let i5 ← U_SIZE + V_SIZE
          if CT_SIZE = i5
          then
            let i6 ← core.slice.Slice.len ek
            let i7 ← parameters.BYTES_PER_RING_ELEMENT
            let i8 ← RANK * i7
            let i9 ← i8 + 32#usize
            if i6 = i9
            then
              if params.eta1 = 2#usize
              then
                if params.eta2 = 2#usize
                then
                  let i10 ← core.slice.Slice.len randomness
                  ok (i10 = 32#usize)
                else
                  if params.eta2 = 3#usize
                  then
                    let i10 ← core.slice.Slice.len randomness
                    ok (i10 = 32#usize)
                  else ok false
              else
                if params.eta1 = 3#usize
                then
                  if params.eta2 = 2#usize
                  then
                    let i10 ← core.slice.Slice.len randomness
                    ok (i10 = 32#usize)
                  else
                    if params.eta2 = 3#usize
                    then
                      let i10 ← core.slice.Slice.len randomness
                      ok (i10 = 32#usize)
                    else ok false
                else ok false
            else ok false
          else ok false
        else ok false
      else ok false
    else ok false
  else ok false

def
  ind_cpa.encrypt.spec (RANK : Std.Usize) (U_SIZE : Std.Usize) (V_SIZE :
                      Std.Usize) (CT_SIZE : Std.Usize)
  (params : parameters.MlKemParams) (ek : Slice Std.U8)
  (message : Array Std.U8 32#usize) (randomness : Slice Std.U8) : Prop :=
  (ind_cpa.encrypt.pre RANK U_SIZE V_SIZE CT_SIZE params ek message
  randomness).holds →
  ⦃ ⌜ True ⌝ ⦄
  ind_cpa.encrypt RANK U_SIZE V_SIZE CT_SIZE params ek message randomness
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ind_cpa::encrypt_unpacked::pre]:
    Source: 'ml-kem/src/ind_cpa.rs', lines 287:0-295:2 -/
@[reducible]
def ind_cpa.encrypt_unpacked.pre
  {RANK : Std.Usize} (U_SIZE : Std.Usize) (V_SIZE : Std.Usize) (CT_SIZE :
  Std.Usize) (params : parameters.MlKemParams)
  (t_as_ntt : Array (Array parameters.FieldElement 256#usize) RANK)
  (A_as_ntt : Array (Array (Array parameters.FieldElement 256#usize) RANK)
  RANK) (message : Array Std.U8 32#usize) (randomness : Slice Std.U8) :
  RustM Bool
  := do
  if RANK <= 4#usize
  then
    if params.rank = RANK
    then
      let i ← RANK * parameters.COEFFICIENTS_IN_RING_ELEMENT
      let i1 ← i * params.du
      let i2 ← i1 / 8#usize
      if U_SIZE = i2
      then
        let i3 ← parameters.COEFFICIENTS_IN_RING_ELEMENT * params.dv
        let i4 ← i3 / 8#usize
        if V_SIZE = i4
        then
          let i5 ← U_SIZE + V_SIZE
          if CT_SIZE = i5
          then
            if params.eta1 = 2#usize
            then
              if params.eta2 = 2#usize
              then
                let i6 ← core.slice.Slice.len randomness
                ok (i6 = 32#usize)
              else
                if params.eta2 = 3#usize
                then
                  let i6 ← core.slice.Slice.len randomness
                  ok (i6 = 32#usize)
                else ok false
            else
              if params.eta1 = 3#usize
              then
                if params.eta2 = 2#usize
                then
                  let i6 ← core.slice.Slice.len randomness
                  ok (i6 = 32#usize)
                else
                  if params.eta2 = 3#usize
                  then
                    let i6 ← core.slice.Slice.len randomness
                    ok (i6 = 32#usize)
                  else ok false
              else ok false
          else ok false
        else ok false
      else ok false
    else ok false
  else ok false

def
  ind_cpa.encrypt_unpacked.spec {RANK : Std.Usize} (U_SIZE : Std.Usize) (V_SIZE
                               : Std.Usize) (CT_SIZE : Std.Usize)
  (params : parameters.MlKemParams)
  (t_as_ntt : Array (Array parameters.FieldElement 256#usize) RANK)
  (A_as_ntt : Array (Array (Array parameters.FieldElement 256#usize) RANK)
  RANK) (message : Array Std.U8 32#usize) (randomness : Slice Std.U8) : Prop :=
  (ind_cpa.encrypt_unpacked.pre U_SIZE V_SIZE CT_SIZE params t_as_ntt A_as_ntt
  message randomness).holds →
  ⦃ ⌜ True ⌝ ⦄
  ind_cpa.encrypt_unpacked U_SIZE V_SIZE CT_SIZE params t_as_ntt A_as_ntt
  message randomness
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ind_cpa::decrypt::pre]:
    Source: 'ml-kem/src/ind_cpa.rs', lines 370:0-374:2 -/
@[reducible]
def ind_cpa.decrypt.pre
  (RANK : Std.Usize) (params : parameters.MlKemParams) (dk : Slice Std.U8)
  (ciphertext : Slice Std.U8) :
  RustM Bool
  := do
  if RANK <= 4#usize
  then
    if params.rank = RANK
    then
      let i ← core.slice.Slice.len dk
      let i1 ← parameters.BYTES_PER_RING_ELEMENT
      let i2 ← RANK * i1
      if i = i2
      then
        let i3 ← core.slice.Slice.len ciphertext
        let i4 ← RANK * parameters.COEFFICIENTS_IN_RING_ELEMENT
        let i5 ← i4 * params.du
        let i6 ← parameters.COEFFICIENTS_IN_RING_ELEMENT * params.dv
        let i7 ← i5 + i6
        let i8 ← i7 / 8#usize
        ok (i3 = i8)
      else ok false
    else ok false
  else ok false

def ind_cpa.decrypt.spec (RANK : Std.Usize) (params : parameters.MlKemParams)
  (dk : Slice Std.U8) (ciphertext : Slice Std.U8) : Prop :=
  (ind_cpa.decrypt.pre RANK params dk ciphertext).holds →
  ⦃ ⌜ True ⌝ ⦄
  ind_cpa.decrypt RANK params dk ciphertext
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ind_cpa::decrypt_unpacked::pre]:
    Source: 'ml-kem/src/ind_cpa.rs', lines 396:0-399:2 -/
@[reducible]
def ind_cpa.decrypt_unpacked.pre
  {RANK : Std.Usize} (params : parameters.MlKemParams)
  (secret_as_ntt : Array (Array parameters.FieldElement 256#usize) RANK)
  (ciphertext : Slice Std.U8) :
  RustM Bool
  := do
  if RANK <= 4#usize
  then
    if params.rank = RANK
    then
      let i ← core.slice.Slice.len ciphertext
      let i1 ← RANK * parameters.COEFFICIENTS_IN_RING_ELEMENT
      let i2 ← i1 * params.du
      let i3 ← parameters.COEFFICIENTS_IN_RING_ELEMENT * params.dv
      let i4 ← i2 + i3
      let i5 ← i4 / 8#usize
      ok (i = i5)
    else ok false
  else ok false

def ind_cpa.decrypt_unpacked.spec {RANK : Std.Usize}
  (params : parameters.MlKemParams)
  (secret_as_ntt : Array (Array parameters.FieldElement 256#usize) RANK)
  (ciphertext : Slice Std.U8) : Prop :=
  (ind_cpa.decrypt_unpacked.pre params secret_as_ntt ciphertext).holds →
  ⦃ ⌜ True ⌝ ⦄
  ind_cpa.decrypt_unpacked params secret_as_ntt ciphertext
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::invert_ntt::ntt_inverse_layer_n::pre]:
    Source: 'ml-kem/src/invert_ntt.rs', lines 56:0-58:2 -/
@[reducible]
def invert_ntt.ntt_inverse_layer_n.pre
  {N : Std.Usize} (p : Array parameters.FieldElement N) (len : Std.Usize)
  (zetas : Slice parameters.FieldElement) :
  RustM Bool
  := do
  if len >= 1#usize
  then
    if len < 1024#usize
    then
      let i ← core.slice.Slice.len zetas
      if i < 1024#usize
      then let i1 ← i * 2#usize
           let i2 ← i1 * len
           ok (i2 = N)
      else ok false
    else ok false
  else ok false

def invert_ntt.ntt_inverse_layer_n.spec {N : Std.Usize}
  (p : Array parameters.FieldElement N) (len : Std.Usize)
  (zetas : Slice parameters.FieldElement) : Prop :=
  (invert_ntt.ntt_inverse_layer_n.pre p len zetas).holds →
  ⦃ ⌜ True ⌝ ⦄
  invert_ntt.ntt_inverse_layer_n p len zetas
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::invert_ntt::ntt_inverse_layer::pre]:
    Source: 'ml-kem/src/invert_ntt.rs', lines 81:0-81:46 -/
@[reducible]
def invert_ntt.ntt_inverse_layer.pre
  (p : Array parameters.FieldElement 256#usize) (layer : Std.Usize) :
  RustM Bool
  := do
  if layer >= 1#usize
  then ok (layer <= 7#usize)
  else ok false

def invert_ntt.ntt_inverse_layer.spec
  (p : Array parameters.FieldElement 256#usize) (layer : Std.Usize) : Prop :=
  (invert_ntt.ntt_inverse_layer.pre p layer).holds →
  ⦃ ⌜ True ⌝ ⦄
  invert_ntt.ntt_inverse_layer p layer
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::matrix::sample_matrix_A::pre]:
    Source: 'ml-kem/src/matrix.rs', lines 97:0-97:58 -/
@[reducible]
def matrix.sample_matrix_A.pre
  (RANK : Std.Usize) (seed_for_A : Slice Std.U8) (transpose : Bool) :
  RustM Bool
  := do
  let i ← core.slice.Slice.len seed_for_A
  if i = 32#usize
  then ok (RANK <= 4#usize)
  else ok false

def matrix.sample_matrix_A.spec (RANK : Std.Usize) (seed_for_A : Slice Std.U8)
  (transpose : Bool) : Prop :=
  (matrix.sample_matrix_A.pre RANK seed_for_A transpose).holds →
  ⦃ ⌜ True ⌝ ⦄
  matrix.sample_matrix_A RANK seed_for_A transpose
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ntt::get_zeta::pre]:
    Source: 'ml-kem/src/ntt.rs', lines 225:0-225:29 -/
@[reducible]
def ntt.get_zeta.pre (i : Std.Usize) : RustM Bool := do
  ok (i < 128#usize)

def ntt.get_zeta.spec (i : Std.Usize) : Prop :=
  (ntt.get_zeta.pre i).holds →
  ⦃ ⌜ True ⌝ ⦄
  ntt.get_zeta i
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ntt::ntt_layer_n::pre]:
    Source: 'ml-kem/src/ntt.rs', lines 253:0-255:2 -/
@[reducible]
def ntt.ntt_layer_n.pre
  {N : Std.Usize} (p : Array parameters.FieldElement N) (len : Std.Usize)
  (zetas : Slice parameters.FieldElement) :
  RustM Bool
  := do
  if len >= 1#usize
  then
    if len < 1024#usize
    then
      let i ← core.slice.Slice.len zetas
      if i < 1024#usize
      then let i1 ← i * 2#usize
           let i2 ← i1 * len
           ok (i2 = N)
      else ok false
    else ok false
  else ok false

def ntt.ntt_layer_n.spec {N : Std.Usize} (p : Array parameters.FieldElement N)
  (len : Std.Usize) (zetas : Slice parameters.FieldElement) : Prop :=
  (ntt.ntt_layer_n.pre p len zetas).holds →
  ⦃ ⌜ True ⌝ ⦄
  ntt.ntt_layer_n p len zetas
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ntt::ntt_layer::pre]:
    Source: 'ml-kem/src/ntt.rs', lines 279:0-279:46 -/
@[reducible]
def ntt.ntt_layer.pre
  (p : Array parameters.FieldElement 256#usize) (layer : Std.Usize) :
  RustM Bool
  := do
  if layer >= 1#usize
  then ok (layer <= 7#usize)
  else ok false

def ntt.ntt_layer.spec (p : Array parameters.FieldElement 256#usize)
  (layer : Std.Usize) : Prop :=
  (ntt.ntt_layer.pre p layer).holds →
  ⦃ ⌜ True ⌝ ⦄
  ntt.ntt_layer p layer
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::ntt::ntt_multiply_n::pre]:
    Source: 'ml-kem/src/ntt.rs', lines 373:0-373:64 -/
@[reducible]
def ntt.ntt_multiply_n.pre
  {N : Std.Usize} (p1 : Array parameters.FieldElement N)
  (p2 : Array parameters.FieldElement N)
  (zetas : Slice parameters.FieldElement) :
  RustM Bool
  := do
  let i ← core.slice.Slice.len zetas
  if i < 1024#usize
  then let i1 ← i * 4#usize
       ok (i1 = N)
  else ok false

def ntt.ntt_multiply_n.spec {N : Std.Usize}
  (p1 : Array parameters.FieldElement N) (p2 : Array parameters.FieldElement N)
  (zetas : Slice parameters.FieldElement) : Prop :=
  (ntt.ntt_multiply_n.pre p1 p2 zetas).holds →
  ⦃ ⌜ True ⌝ ⦄
  ntt.ntt_multiply_n p1 p2 zetas
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::sampling::sample_ntt::pre]:
    Source: 'ml-kem/src/sampling.rs', lines 71:0-76:2 -/
@[reducible]
def sampling.sample_ntt.pre
  (N : Std.Usize) (N8 : Std.Usize) {N12 : Std.Usize} (N96 : Std.Usize)
  (bytes : Array Std.U8 N12) :
  RustM Bool
  := do
  let i ← serialize.MAX_BYTES / 96#usize
  if N <= i
  then
    let i1 ← N * 8#usize
    if N8 = i1
    then
      let i2 ← N * 12#usize
      if N12 = i2
      then let i3 ← N12 * 8#usize
           ok (N96 = i3)
      else ok false
    else ok false
  else ok false

def
  sampling.sample_ntt.spec (N : Std.Usize) (N8 : Std.Usize) {N12 : Std.Usize}
                          (N96 : Std.Usize) (bytes : Array Std.U8 N12)
  : Prop :=
  (sampling.sample_ntt.pre N N8 N96 bytes).holds →
  ⦃ ⌜ True ⌝ ⦄
  sampling.sample_ntt N N8 N96 bytes
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::sampling::sum_coins::pre]:
    Source: 'ml-kem/src/sampling.rs', lines 96:0-96:52 -/
@[reducible]
def sampling.sum_coins.pre
  (eta : Std.Usize) (coins : Slice Bool) : RustM Bool := do
  if eta <= 4#usize
  then let i ← core.slice.Slice.len coins
       ok (i = eta)
  else ok false

/-- [hacspec_ml_kem::sampling::sum_coins::post]:
    Source: 'ml-kem/src/sampling.rs', lines 97:0-97:59 -/
@[reducible]
def sampling.sum_coins.post
  (eta : Std.Usize) (coins : Slice Bool) (r : parameters.FieldElement) :
  RustM Bool
  := do
  let i ← lift (UScalar.cast .U16 eta)
  ok (r.val <= i)

def sampling.sum_coins.spec (eta : Std.Usize) (coins : Slice Bool) : Prop :=
  (sampling.sum_coins.pre eta coins).holds →
  ⦃ ⌜ True ⌝ ⦄
  sampling.sum_coins eta coins
  ⦃ ⇓ res => ⌜ (sampling.sum_coins.post eta coins res).holds ⌝ ⦄


/-- [hacspec_ml_kem::sampling::sample_poly_cbd::pre]:
    Source: 'ml-kem/src/sampling.rs', lines 157:0-157:74 -/
@[reducible]
def sampling.sample_poly_cbd.pre
  {ETA64 : Std.Usize} (ETA512 : Std.Usize) (eta : Std.Usize)
  (bytes : Array Std.U8 ETA64) :
  RustM Bool
  := do
  if eta <= 4#usize
  then
    let i ← eta * 64#usize
    if ETA64 = i
    then let i1 ← eta * 512#usize
         ok (ETA512 = i1)
    else ok false
  else ok false

def sampling.sample_poly_cbd.spec {ETA64 : Std.Usize} (ETA512 : Std.Usize)
  (eta : Std.Usize) (bytes : Array Std.U8 ETA64) : Prop :=
  (sampling.sample_poly_cbd.pre ETA512 eta bytes).holds →
  ⦃ ⌜ True ⌝ ⦄
  sampling.sample_poly_cbd ETA512 eta bytes
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::serialize::bytes_to_bits::pre]:
    Source: 'ml-kem/src/serialize.rs', lines 27:0-27:46 -/
@[reducible]
def serialize.bytes_to_bits.pre
  {N : Std.Usize} (N8 : Std.Usize) (bytes : Array Std.U8 N) : RustM Bool := do
  if N < 16384#usize
  then let i ← N * 8#usize
       ok (N8 = i)
  else ok false

def serialize.bytes_to_bits.spec {N : Std.Usize} (N8 : Std.Usize)
  (bytes : Array Std.U8 N) : Prop :=
  (serialize.bytes_to_bits.pre N8 bytes).holds →
  ⦃ ⌜ True ⌝ ⦄
  serialize.bytes_to_bits N8 bytes
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::serialize::bits_to_bytes::pre]:
    Source: 'ml-kem/src/serialize.rs', lines 53:0-53:46 -/
@[reducible]
def serialize.bits_to_bytes.pre
  (N : Std.Usize) {N8 : Std.Usize} (bv : Array Bool N8) : RustM Bool := do
  if N < 16384#usize
  then let i ← N * 8#usize
       ok (N8 = i)
  else ok false

def serialize.bits_to_bytes.spec (N : Std.Usize) {N8 : Std.Usize}
  (bv : Array Bool N8) : Prop :=
  (serialize.bits_to_bytes.pre N bv).holds →
  ⦃ ⌜ True ⌝ ⦄
  serialize.bits_to_bytes N bv
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::serialize::bitvector_from_bounded_ints::pre]:
    Source: 'ml-kem/src/serialize.rs', lines 108:0-108:75 -/
@[reducible]
def serialize.bitvector_from_bounded_ints.pre
  {N : Std.Usize} (Nd : Std.Usize) (input : Array Std.U16 N) (d : Std.Usize) :
  RustM Bool
  := do
  if N < 16384#usize
  then
    if d <= parameters.BITS_PER_COEFFICIENT
    then let i ← N * d
         ok (Nd = i)
    else ok false
  else ok false

def serialize.bitvector_from_bounded_ints.spec {N : Std.Usize} (Nd : Std.Usize)
  (input : Array Std.U16 N) (d : Std.Usize) : Prop :=
  (serialize.bitvector_from_bounded_ints.pre Nd input d).holds →
  ⦃ ⌜ True ⌝ ⦄
  serialize.bitvector_from_bounded_ints Nd input d
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::serialize::byte_encode::pre]:
    Source: 'ml-kem/src/serialize.rs', lines 118:0-118:83 -/
@[reducible]
def serialize.byte_encode.pre
  (D32 : Std.Usize) (D256 : Std.Usize)
  (p : Array parameters.FieldElement 256#usize) (d : Std.Usize) :
  RustM Bool
  := do
  if d <= parameters.BITS_PER_COEFFICIENT
  then
    let i ← 32#usize * d
    if D32 = i
    then let i1 ← 256#usize * d
         ok (D256 = i1)
    else ok false
  else ok false

def serialize.byte_encode.spec (D32 : Std.Usize) (D256 : Std.Usize)
  (p : Array parameters.FieldElement 256#usize) (d : Std.Usize) : Prop :=
  (serialize.byte_encode.pre D32 D256 p d).holds →
  ⦃ ⌜ True ⌝ ⦄
  serialize.byte_encode D32 D256 p d
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::serialize::bitvector_to_bounded_ints::pre]:
    Source: 'ml-kem/src/serialize.rs', lines 155:0-155:75 -/
@[reducible]
def serialize.bitvector_to_bounded_ints.pre
  (N : Std.Usize) {Nd : Std.Usize} (input : Array Bool Nd) (d : Std.Usize) :
  RustM Bool
  := do
  if N < 16384#usize
  then
    if d <= parameters.BITS_PER_COEFFICIENT
    then let i ← N * d
         ok (Nd = i)
    else ok false
  else ok false

/-- [hacspec_ml_kem::serialize::bitvector_to_bounded_ints::post]:
    Source: 'ml-kem/src/serialize.rs', lines 156:0-157:83 -/
@[reducible]
def serialize.bitvector_to_bounded_ints.post
  {N : Std.Usize} {Nd : Std.Usize} (input : Array Bool Nd) (d : Std.Usize)
  (result : Array Std.U16 N) :
  RustM hax_lib.prop.Prop
  := do
  hax_lib.prop.forall (core.convert.Into.Blanket (core.convert.From.Blanket
    hax_lib.prop.Prop))
    (serialize.__5.ensures.closure.Insts.CoreOpsFunctionFnTupleUsizeProp N Nd)
    (result, d)

def serialize.bitvector_to_bounded_ints.spec (N : Std.Usize) {Nd : Std.Usize}
  (input : Array Bool Nd) (d : Std.Usize) : Prop :=
  (serialize.bitvector_to_bounded_ints.pre N input d).holds →
  ⦃ ⌜ True ⌝ ⦄
  serialize.bitvector_to_bounded_ints N input d
  ⦃ ⇓ res =>
  ⌜ (serialize.bitvector_to_bounded_ints.post input d res).holds ⌝ ⦄


/-- [hacspec_ml_kem::serialize::byte_decode_generic::pre]:
    Source: 'ml-kem/src/serialize.rs', lines 194:0-194:137 -/
@[reducible]
def serialize.byte_decode_generic.pre
  (N : Std.Usize) (N8 : Std.Usize) {Nd : Std.Usize} (Nd8 : Std.Usize)
  (b : Array Std.U8 Nd) (d : Std.Usize) :
  RustM Bool
  := do
  if d > 0#usize
  then
    if d <= parameters.BITS_PER_COEFFICIENT
    then
      let i ← 16384#usize / d
      if N < i
      then
        let i1 ← 16384#usize / 8#usize
        if N < i1
        then
          let i2 ← N * 8#usize
          if N8 = i2
          then
            let i3 ← N * d
            if Nd = i3
            then let i4 ← Nd * 8#usize
                 ok (Nd8 = i4)
            else ok false
          else ok false
        else ok false
      else ok false
    else ok false
  else ok false

/-- [hacspec_ml_kem::serialize::byte_decode_generic::post]:
    Source: 'ml-kem/src/serialize.rs', lines 195:0-196:84 -/
@[reducible]
def serialize.byte_decode_generic.post
  (N : Std.Usize) {N8 : Std.Usize} {Nd : Std.Usize} (Nd8 : Std.Usize)
  (b : Array Std.U8 Nd) (d : Std.Usize) (result : Array Std.U16 N8) :
  RustM hax_lib.prop.Prop
  := do
  hax_lib.prop.forall (core.convert.Into.Blanket (core.convert.From.Blanket
    hax_lib.prop.Prop))
    (serialize.__7.ensures.closure.Insts.CoreOpsFunctionFnTupleUsizeProp N N8
    Nd Nd8) (result, d)

def
  serialize.byte_decode_generic.spec (N : Std.Usize) (N8 : Std.Usize) {Nd :
                                    Std.Usize} (Nd8 : Std.Usize)
  (b : Array Std.U8 Nd) (d : Std.Usize) : Prop :=
  (serialize.byte_decode_generic.pre N N8 Nd8 b d).holds →
  ⦃ ⌜ True ⌝ ⦄
  serialize.byte_decode_generic N N8 Nd8 b d
  ⦃ ⇓ res =>
  ⌜ (serialize.byte_decode_generic.post N Nd8 b d res).holds ⌝ ⦄


/-- [hacspec_ml_kem::serialize::byte_decode::pre]:
    Source: 'ml-kem/src/serialize.rs', lines 209:0-209:113 -/
@[reducible]
def serialize.byte_decode.pre
  {D32 : Std.Usize} (D256 : Std.Usize) (b : Array Std.U8 D32) (d : Std.Usize) :
  RustM Bool
  := do
  if d > 0#usize
  then
    if d <= parameters.BITS_PER_COEFFICIENT
    then
      let s ← lift (Array.to_slice b)
      let i ← core.slice.Slice.len s
      let i1 ← 32#usize * d
      if i = i1
      then
        if D32 = i1
        then let i2 ← 256#usize * d
             ok (D256 = i2)
        else ok false
      else ok false
    else ok false
  else ok false

/-- [hacspec_ml_kem::serialize::byte_decode::post]:
    Source: 'ml-kem/src/serialize.rs', lines 210:0-210:127 -/
@[reducible]
def serialize.byte_decode.post
  {D32 : Std.Usize} (D256 : Std.Usize) (b : Array Std.U8 D32) (d : Std.Usize)
  (result : Array parameters.FieldElement 256#usize) :
  RustM hax_lib.prop.Prop
  := do
  hax_lib.prop.forall (core.convert.Into.Blanket (core.convert.From.Blanket
    hax_lib.prop.Prop))
    (serialize.__9.ensures.closure.Insts.CoreOpsFunctionFnTupleUsizeProp D32
    D256) (result, d)

def serialize.byte_decode.spec {D32 : Std.Usize} (D256 : Std.Usize)
  (b : Array Std.U8 D32) (d : Std.Usize) : Prop :=
  (serialize.byte_decode.pre D256 b d).holds →
  ⦃ ⌜ True ⌝ ⦄
  serialize.byte_decode D256 b d
  ⦃ ⇓ res => ⌜ (serialize.byte_decode.post D256 b d res).holds ⌝ ⦄


/-- [hacspec_ml_kem::serialize::serialize_secret_key_into::pre]:
    Source: 'ml-kem/src/serialize.rs', lines 225:0-225:77 -/
@[reducible]
def serialize.serialize_secret_key_into.pre
  {RANK : Std.Usize}
  (vector : Array (Array parameters.FieldElement 256#usize) RANK)
  (out : Slice Std.U8) :
  RustM Bool
  := do
  if RANK <= 4#usize
  then
    let i ← core.slice.Slice.len out
    let i1 ← parameters.BYTES_PER_RING_ELEMENT
    let i2 ← RANK * i1
    ok (i = i2)
  else ok false

def serialize.serialize_secret_key_into.spec {RANK : Std.Usize}
  (vector : Array (Array parameters.FieldElement 256#usize) RANK)
  (out : Slice Std.U8) : Prop :=
  (serialize.serialize_secret_key_into.pre vector out).holds →
  ⦃ ⌜ True ⌝ ⦄
  serialize.serialize_secret_key_into vector out
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::serialize::vector_decode_12::pre]:
    Source: 'ml-kem/src/serialize.rs', lines 237:0-237:81 -/
@[reducible]
def serialize.vector_decode_12.pre
  (RANK : Std.Usize) (encoded : Slice Std.U8) : RustM Bool := do
  if RANK <= 4#usize
  then
    let i ← core.slice.Slice.len encoded
    let i1 ← parameters.BYTES_PER_RING_ELEMENT
    let i2 ← RANK * i1
    ok (i = i2)
  else ok false

def serialize.vector_decode_12.spec (RANK : Std.Usize) (encoded : Slice Std.U8)
  : Prop :=
  (serialize.vector_decode_12.pre RANK encoded).holds →
  ⦃ ⌜ True ⌝ ⦄
  serialize.vector_decode_12 RANK encoded
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::serialize::byte_encode_into::pre]:
    Source: 'ml-kem/src/serialize.rs', lines 248:0-248:106 -/
@[reducible]
def serialize.byte_encode_into.pre
  (p : Array parameters.FieldElement 256#usize) (d : Std.Usize)
  (out : Slice Std.U8) :
  RustM Bool
  := do
  if d = 1#usize
  then let i ← core.slice.Slice.len out
       let i1 ← 32#usize * d
       ok (i = i1)
  else
    if d = 4#usize
    then let i ← core.slice.Slice.len out
         let i1 ← 32#usize * d
         ok (i = i1)
    else
      if d = 5#usize
      then
        let i ← core.slice.Slice.len out
        let i1 ← 32#usize * d
        ok (i = i1)
      else
        if d = 10#usize
        then
          let i ← core.slice.Slice.len out
          let i1 ← 32#usize * d
          ok (i = i1)
        else
          if d = 11#usize
          then
            let i ← core.slice.Slice.len out
            let i1 ← 32#usize * d
            ok (i = i1)
          else
            if d = 12#usize
            then
              let i ← core.slice.Slice.len out
              let i1 ← 32#usize * d
              ok (i = i1)
            else ok false

def serialize.byte_encode_into.spec
  (p : Array parameters.FieldElement 256#usize) (d : Std.Usize)
  (out : Slice Std.U8) : Prop :=
  (serialize.byte_encode_into.pre p d out).holds →
  ⦃ ⌜ True ⌝ ⦄
  serialize.byte_encode_into p d out
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::serialize::byte_decode_dyn::pre]:
    Source: 'ml-kem/src/serialize.rs', lines 263:0-263:104 -/
@[reducible]
def serialize.byte_decode_dyn.pre
  (b : Slice Std.U8) (d : Std.Usize) : RustM Bool := do
  if d = 1#usize
  then let i ← core.slice.Slice.len b
       let i1 ← 32#usize * d
       ok (i = i1)
  else
    if d = 4#usize
    then let i ← core.slice.Slice.len b
         let i1 ← 32#usize * d
         ok (i = i1)
    else
      if d = 5#usize
      then let i ← core.slice.Slice.len b
           let i1 ← 32#usize * d
           ok (i = i1)
      else
        if d = 10#usize
        then
          let i ← core.slice.Slice.len b
          let i1 ← 32#usize * d
          ok (i = i1)
        else
          if d = 11#usize
          then
            let i ← core.slice.Slice.len b
            let i1 ← 32#usize * d
            ok (i = i1)
          else
            if d = 12#usize
            then
              let i ← core.slice.Slice.len b
              let i1 ← 32#usize * d
              ok (i = i1)
            else ok false

def serialize.byte_decode_dyn.spec (b : Slice Std.U8) (d : Std.Usize) : Prop :=
  (serialize.byte_decode_dyn.pre b d).holds →
  ⦃ ⌜ True ⌝ ⦄
  serialize.byte_decode_dyn b d
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::serialize::compress_then_serialize_u_into::pre]:
    Source: 'ml-kem/src/serialize.rs', lines 314:0-318:2 -/
@[reducible]
def serialize.compress_then_serialize_u_into.pre
  {RANK : Std.Usize} (u : Array (Array parameters.FieldElement 256#usize) RANK)
  (du : Std.Usize) (out : Slice Std.U8) :
  RustM Bool
  := do
  if RANK <= 4#usize
  then
    if du = 10#usize
    then
      let i ← core.slice.Slice.len out
      let i1 ← RANK * parameters.COEFFICIENTS_IN_RING_ELEMENT
      let i2 ← i1 * du
      let i3 ← i2 / 8#usize
      ok (i = i3)
    else
      if du = 11#usize
      then
        let i ← core.slice.Slice.len out
        let i1 ← RANK * parameters.COEFFICIENTS_IN_RING_ELEMENT
        let i2 ← i1 * du
        let i3 ← i2 / 8#usize
        ok (i = i3)
      else ok false
  else ok false

def serialize.compress_then_serialize_u_into.spec {RANK : Std.Usize}
  (u : Array (Array parameters.FieldElement 256#usize) RANK) (du : Std.Usize)
  (out : Slice Std.U8) : Prop :=
  (serialize.compress_then_serialize_u_into.pre u du out).holds →
  ⦃ ⌜ True ⌝ ⦄
  serialize.compress_then_serialize_u_into u du out
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::serialize::compress_then_serialize_u::pre]:
    Source: 'ml-kem/src/serialize.rs', lines 345:0-345:117 -/
@[reducible]
def serialize.compress_then_serialize_u.pre
  {RANK : Std.Usize} (U_SIZE : Std.Usize)
  (u : Array (Array parameters.FieldElement 256#usize) RANK) (du : Std.Usize) :
  RustM Bool
  := do
  if RANK <= 4#usize
  then
    if du = 10#usize
    then
      let i ← RANK * parameters.COEFFICIENTS_IN_RING_ELEMENT
      let i1 ← i * du
      let i2 ← i1 / 8#usize
      ok (U_SIZE = i2)
    else
      if du = 11#usize
      then
        let i ← RANK * parameters.COEFFICIENTS_IN_RING_ELEMENT
        let i1 ← i * du
        let i2 ← i1 / 8#usize
        ok (U_SIZE = i2)
      else ok false
  else ok false

def
  serialize.compress_then_serialize_u.spec {RANK : Std.Usize} (U_SIZE :
                                          Std.Usize)
  (u : Array (Array parameters.FieldElement 256#usize) RANK) (du : Std.Usize)
  : Prop :=
  (serialize.compress_then_serialize_u.pre U_SIZE u du).holds →
  ⦃ ⌜ True ⌝ ⦄
  serialize.compress_then_serialize_u U_SIZE u du
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::serialize::compress_then_serialize_v::pre]:
    Source: 'ml-kem/src/serialize.rs', lines 358:0-358:95 -/
@[reducible]
def serialize.compress_then_serialize_v.pre
  (V_SIZE : Std.Usize) (v : Array parameters.FieldElement 256#usize)
  (dv : Std.Usize) :
  RustM Bool
  := do
  if dv = 4#usize
  then
    let i ← parameters.COEFFICIENTS_IN_RING_ELEMENT * dv
    let i1 ← i / 8#usize
    ok (V_SIZE = i1)
  else
    if dv = 5#usize
    then
      let i ← parameters.COEFFICIENTS_IN_RING_ELEMENT * dv
      let i1 ← i / 8#usize
      ok (V_SIZE = i1)
    else ok false

def serialize.compress_then_serialize_v.spec (V_SIZE : Std.Usize)
  (v : Array parameters.FieldElement 256#usize) (dv : Std.Usize) : Prop :=
  (serialize.compress_then_serialize_v.pre V_SIZE v dv).holds →
  ⦃ ⌜ True ⌝ ⦄
  serialize.compress_then_serialize_v V_SIZE v dv
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::serialize::deserialize_then_decompress_u::pre]:
    Source: 'ml-kem/src/serialize.rs', lines 368:0-368:127 -/
@[reducible]
def serialize.deserialize_then_decompress_u.pre
  (RANK : Std.Usize) (ciphertext : Slice Std.U8) (du : Std.Usize) :
  RustM Bool
  := do
  if RANK <= 4#usize
  then
    if du = 10#usize
    then
      let i ← core.slice.Slice.len ciphertext
      let i1 ← RANK * parameters.COEFFICIENTS_IN_RING_ELEMENT
      let i2 ← i1 * du
      let i3 ← i2 / 8#usize
      ok (i = i3)
    else
      if du = 11#usize
      then
        let i ← core.slice.Slice.len ciphertext
        let i1 ← RANK * parameters.COEFFICIENTS_IN_RING_ELEMENT
        let i2 ← i1 * du
        let i3 ← i2 / 8#usize
        ok (i = i3)
      else ok false
  else ok false

def serialize.deserialize_then_decompress_u.spec (RANK : Std.Usize)
  (ciphertext : Slice Std.U8) (du : Std.Usize) : Prop :=
  (serialize.deserialize_then_decompress_u.pre RANK ciphertext du).holds →
  ⦃ ⌜ True ⌝ ⦄
  serialize.deserialize_then_decompress_u RANK ciphertext du
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::serialize::deserialize_then_decompress_u_then_ntt::pre]:
    Source: 'ml-kem/src/serialize.rs', lines 389:0-389:127 -/
@[reducible]
def serialize.deserialize_then_decompress_u_then_ntt.pre
  (RANK : Std.Usize) (ciphertext : Slice Std.U8) (du : Std.Usize) :
  RustM Bool
  := do
  if RANK <= 4#usize
  then
    if du = 10#usize
    then
      let i ← core.slice.Slice.len ciphertext
      let i1 ← RANK * parameters.COEFFICIENTS_IN_RING_ELEMENT
      let i2 ← i1 * du
      let i3 ← i2 / 8#usize
      ok (i = i3)
    else
      if du = 11#usize
      then
        let i ← core.slice.Slice.len ciphertext
        let i1 ← RANK * parameters.COEFFICIENTS_IN_RING_ELEMENT
        let i2 ← i1 * du
        let i3 ← i2 / 8#usize
        ok (i = i3)
      else ok false
  else ok false

def serialize.deserialize_then_decompress_u_then_ntt.spec (RANK : Std.Usize)
  (ciphertext : Slice Std.U8) (du : Std.Usize) : Prop :=
  (serialize.deserialize_then_decompress_u_then_ntt.pre RANK ciphertext
  du).holds →
  ⦃ ⌜ True ⌝ ⦄
  serialize.deserialize_then_decompress_u_then_ntt RANK ciphertext du
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::serialize::deserialize_then_decompress_v::pre]:
    Source: 'ml-kem/src/serialize.rs', lines 400:0-400:105 -/
@[reducible]
def serialize.deserialize_then_decompress_v.pre
  (serialized : Slice Std.U8) (dv : Std.Usize) : RustM Bool := do
  if dv = 4#usize
  then
    let i ← core.slice.Slice.len serialized
    let i1 ← parameters.COEFFICIENTS_IN_RING_ELEMENT * dv
    let i2 ← i1 / 8#usize
    ok (i = i2)
  else
    if dv = 5#usize
    then
      let i ← core.slice.Slice.len serialized
      let i1 ← parameters.COEFFICIENTS_IN_RING_ELEMENT * dv
      let i2 ← i1 / 8#usize
      ok (i = i2)
    else ok false

def serialize.deserialize_then_decompress_v.spec (serialized : Slice Std.U8)
  (dv : Std.Usize) : Prop :=
  (serialize.deserialize_then_decompress_v.pre serialized dv).holds →
  ⦃ ⌜ True ⌝ ⦄
  serialize.deserialize_then_decompress_v serialized dv
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::serialize::deserialize_ring_elements_reduced::pre]:
    Source: 'ml-kem/src/serialize.rs', lines 409:0-409:81 -/
@[reducible]
def serialize.deserialize_ring_elements_reduced.pre
  (RANK : Std.Usize) (encoded : Slice Std.U8) : RustM Bool := do
  if RANK <= 4#usize
  then
    let i ← core.slice.Slice.len encoded
    let i1 ← parameters.BYTES_PER_RING_ELEMENT
    let i2 ← RANK * i1
    ok (i = i2)
  else ok false

def serialize.deserialize_ring_elements_reduced.spec (RANK : Std.Usize)
  (encoded : Slice Std.U8) : Prop :=
  (serialize.deserialize_ring_elements_reduced.pre RANK encoded).holds →
  ⦃ ⌜ True ⌝ ⦄
  serialize.deserialize_ring_elements_reduced RANK encoded
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::serialize::serialize_secret_key::pre]:
    Source: 'ml-kem/src/serialize.rs', lines 416:0-416:74 -/
@[reducible]
def serialize.serialize_secret_key.pre
  {RANK : Std.Usize} (T_SIZE : Std.Usize)
  (vector : Array (Array parameters.FieldElement 256#usize) RANK) :
  RustM Bool
  := do
  if RANK <= 4#usize
  then
    let i ← parameters.BYTES_PER_RING_ELEMENT
    let i1 ← RANK * i
    ok (T_SIZE = i1)
  else ok false

def serialize.serialize_secret_key.spec {RANK : Std.Usize} (T_SIZE : Std.Usize)
  (vector : Array (Array parameters.FieldElement 256#usize) RANK) : Prop :=
  (serialize.serialize_secret_key.pre T_SIZE vector).holds →
  ⦃ ⌜ True ⌝ ⦄
  serialize.serialize_secret_key T_SIZE vector
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::serialize::serialize_public_key::pre]:
    Source: 'ml-kem/src/serialize.rs', lines 433:0-433:106 -/
@[reducible]
def serialize.serialize_public_key.pre
  {RANK : Std.Usize} (EK_SIZE : Std.Usize)
  (t_as_ntt : Array (Array parameters.FieldElement 256#usize) RANK)
  (seed_for_A : Slice Std.U8) :
  RustM Bool
  := do
  if RANK <= 4#usize
  then
    let i ← parameters.BYTES_PER_RING_ELEMENT
    let i1 ← RANK * i
    let i2 ← i1 + 32#usize
    if EK_SIZE = i2
    then let i3 ← core.slice.Slice.len seed_for_A
         ok (i3 >= 32#usize)
    else ok false
  else ok false

def
  serialize.serialize_public_key.spec {RANK : Std.Usize} (EK_SIZE : Std.Usize)
  (t_as_ntt : Array (Array parameters.FieldElement 256#usize) RANK)
  (seed_for_A : Slice Std.U8) : Prop :=
  (serialize.serialize_public_key.pre EK_SIZE t_as_ntt seed_for_A).holds →
  ⦃ ⌜ True ⌝ ⦄
  serialize.serialize_public_key EK_SIZE t_as_ntt seed_for_A
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_ml_kem::parameters::{hacspec_ml_kem::parameters::FieldElement}::new::pre]:
    Source: 'ml-kem/src/parameters.rs', lines 321:4-321:45 -/
@[reducible]
def parameters.FieldElement.new.pre (val : Std.U16) : RustM Bool := do
  ok (val < parameters.FIELD_MODULUS)

def parameters.FieldElement.new.spec (val : Std.U16) : Prop :=
  (parameters.FieldElement.new.pre val).holds →
  ⦃ ⌜ True ⌝ ⦄
  parameters.FieldElement.new val
  ⦃ ⇓ res => ⌜ True ⌝ ⦄

end hacspec_ml_kem
