-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- [hacspec_ml_dsa]: external functions.
-- Hand-written external models (mirrors the ml-kem spec's FunsExternal). The
-- CoreModels library (hax-lean v0.2.0) provides most bridges natively; only the
-- still-missing ones are added below.
import Aeneas
import CoreModels
import HacspecMlDsa.Extraction.Types
import HacspecSha3.Extraction.Funs
open CoreModels Aeneas
open Aeneas.Std hiding namespace core alloc
open Result ControlFlow Error
open Std.Do
set_option linter.dupNamespace false
set_option linter.hashCommand false
set_option linter.unusedVariables false

set_option maxHeartbeats 1000000
set_option maxRecDepth 2048
open hacspec_ml_dsa

noncomputable section

namespace CoreModels.core

/-- `Try::branch` for `Option<T>` — the `?`-operator desugar on options.
    `Some x → Continue x`, `None → Break None`. -/
def option.Option.Insts.CoreOpsTry_traitTry.branch
    {T : Type} (o : option.Option T) :
    Aeneas.Std.Result (ops.control_flow.ControlFlow (option.Option convert.Infallible) T) :=
  match o with
  | .Some x => Aeneas.Std.Result.ok (.Continue x)
  | .None => Aeneas.Std.Result.ok (.Break .None)

/-- `FromResidual::from_residual` for `Option<T>` from residual
    `Option<Infallible>` (the other half of the `?` desugar). The residual is
    always `None` (Infallible is uninhabited), so this yields `None`. -/
def option.Option.Insts.CoreOpsTry_traitFromResidualOptionInfallible.from_residual
    (T : Type) (_residual : option.Option convert.Infallible) :
    Aeneas.Std.Result (option.Option T) :=
  Aeneas.Std.Result.ok .None

/-- `PartialEq` not-equal for arrays. Forwards to the array `eq` and negates. -/
def Array.Insts.CoreCmpPartialEqArray.ne
    {T U : Type} {N : Std.Usize} (cmpPartialEqInst : cmp.PartialEq T U)
    (self : Aeneas.Std.Array T N) (other : Aeneas.Std.Array U N) :
    Aeneas.Std.Result Bool := do
  let eq ← Array.Insts.CoreCmpPartialEqArray.eq cmpPartialEqInst self other
  Aeneas.Std.Result.ok (!eq)

end CoreModels.core

end
