-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- [hacspec_sha3]: specs (statements of correctness)
import Aeneas
import CoreModels
import Hax
import HacspecSha3.Extraction.Types
import HacspecSha3.Extraction.Funs
open CoreModels Aeneas
open Aeneas.Std hiding namespace core alloc
open RustM ControlFlow Error
open Std.Do
set_option linter.dupNamespace false
set_option linter.hashCommand false
set_option linter.unusedVariables false

/- You can set the `maxHeartbeats` value with the `-max-heartbeats` CLI option -/
set_option maxHeartbeats 1000000

/- You can set the `maxRecDepth` value with the `-max-recdepth` CLI option -/
set_option maxRecDepth 2048

/- You can remove the following line by using the CLI option `-all-computable`: -/
noncomputable section

namespace hacspec_sha3


/-- [hacspec_sha3::keccak_f::get::pre]:
    Source: 'sha3/src/keccak_f.rs', lines 15:16-15:49 -/
@[reducible]
def keccak_f.get.pre
  (state : Array Std.U64 25#usize) (x : Std.Usize) (y : Std.Usize) :
  RustM Bool
  := do
  if x < 5#usize
  then ok (y < 5#usize)
  else ok false

def keccak_f.get.spec (state : Array Std.U64 25#usize) (x : Std.Usize)
  (y : Std.Usize) : Prop :=
  (keccak_f.get.pre state x y).holds →
  ⦃ ⌜ True ⌝ ⦄
  keccak_f.get state x y
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_sha3::keccak_f::iota::pre]:
    Source: 'sha3/src/keccak_f.rs', lines 117:16-117:45 -/
@[reducible]
def keccak_f.iota.pre
  (state : Array Std.U64 25#usize) (round : Std.Usize) : RustM Bool := do
  ok (round < 24#usize)

def keccak_f.iota.spec (state : Array Std.U64 25#usize) (round : Std.Usize)
  : Prop :=
  (keccak_f.iota.pre state round).holds →
  ⦃ ⌜ True ⌝ ⦄
  keccak_f.iota state round
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_sha3::sha3::shake128::pre]:
    Source: 'sha3/src/sha3.rs', lines 42:16-42:55 -/
@[reducible]
def sha3.shake128.pre
  (N : Std.Usize) (message : Slice Std.U8) : RustM Bool := do
  let i ← core.num.Usize.MAX - 200#usize
  ok (N < i)

def sha3.shake128.spec (N : Std.Usize) (message : Slice Std.U8) : Prop :=
  (sha3.shake128.pre N message).holds →
  ⦃ ⌜ True ⌝ ⦄
  sha3.shake128 N message
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_sha3::sha3::shake256::pre]:
    Source: 'sha3/src/sha3.rs', lines 52:16-52:55 -/
@[reducible]
def sha3.shake256.pre
  (N : Std.Usize) (message : Slice Std.U8) : RustM Bool := do
  let i ← core.num.Usize.MAX - 200#usize
  ok (N < i)

def sha3.shake256.spec (N : Std.Usize) (message : Slice Std.U8) : Prop :=
  (sha3.shake256.pre N message).holds →
  ⦃ ⌜ True ⌝ ⦄
  sha3.shake256 N message
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_sha3::sponge::xor_block_into_state::pre]:
    Source: 'sha3/src/sponge.rs', lines 16:16-16:86 -/
@[reducible]
def sponge.xor_block_into_state.pre
  (state : Array Std.U64 25#usize) (block : Slice Std.U8) (rate : Std.Usize) :
  RustM Bool
  := do
  if rate <= 200#usize
  then
    let i ← rate % 8#usize
    if i = 0#usize
    then let i1 ← core.slice.Slice.len block
         ok (i1 >= rate)
    else ok false
  else ok false

def sponge.xor_block_into_state.spec (state : Array Std.U64 25#usize)
  (block : Slice Std.U8) (rate : Std.Usize) : Prop :=
  (sponge.xor_block_into_state.pre state block rate).holds →
  ⦃ ⌜ True ⌝ ⦄
  sponge.xor_block_into_state state block rate
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_sha3::sponge::squeeze_state::pre]:
    Source: 'sha3/src/sponge.rs', lines 32:16-32:104 -/
@[reducible]
def sponge.squeeze_state.pre
  {OUTPUT_LEN : Std.Usize} (state : Array Std.U64 25#usize)
  (output : Array Std.U8 OUTPUT_LEN) (out_offset : Std.Usize) (len : Std.Usize)
  :
  RustM Bool
  := do
  if len <= 200#usize
  then
    let s ← lift (Array.to_slice output)
    let i ← core.slice.Slice.len s
    if i >= len
    then
      let s1 ← lift (Array.to_slice output)
      let i1 ← core.slice.Slice.len s1
      let i2 ← i1 - len
      ok (out_offset <= i2)
    else ok false
  else ok false

def sponge.squeeze_state.spec {OUTPUT_LEN : Std.Usize}
  (state : Array Std.U64 25#usize) (output : Array Std.U8 OUTPUT_LEN)
  (out_offset : Std.Usize) (len : Std.Usize) : Prop :=
  (sponge.squeeze_state.pre state output out_offset len).holds →
  ⦃ ⌜ True ⌝ ⦄
  sponge.squeeze_state state output out_offset len
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_sha3::sponge::absorb_block::pre]:
    Source: 'sha3/src/sponge.rs', lines 47:16-47:86 -/
@[reducible]
def sponge.absorb_block.pre
  (state : Array Std.U64 25#usize) (block : Slice Std.U8) (rate : Std.Usize) :
  RustM Bool
  := do
  if rate <= 200#usize
  then
    let i ← rate % 8#usize
    if i = 0#usize
    then let i1 ← core.slice.Slice.len block
         ok (i1 = rate)
    else ok false
  else ok false

def sponge.absorb_block.spec (state : Array Std.U64 25#usize)
  (block : Slice Std.U8) (rate : Std.Usize) : Prop :=
  (sponge.absorb_block.pre state block rate).holds →
  ⦃ ⌜ True ⌝ ⦄
  sponge.absorb_block state block rate
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_sha3::sponge::pad_last_block::pre]:
    Source: 'sha3/src/sponge.rs', lines 57:16-58:95 -/
@[reducible]
def sponge.pad_last_block.pre
  (message : Slice Std.U8) (msg_offset : Std.Usize) (remaining : Std.Usize)
  (rate : Std.Usize) (delim : Std.U8) :
  RustM Bool
  := do
  if rate > 0#usize
  then
    if rate <= 200#usize
    then
      let i ← rate % 8#usize
      if i = 0#usize
      then
        if remaining < rate
        then
          let i1 ← core.slice.Slice.len message
          if msg_offset <= i1
          then let i2 ← i1 - msg_offset
               ok (remaining <= i2)
          else ok false
        else ok false
      else ok false
    else ok false
  else ok false

def sponge.pad_last_block.spec (message : Slice Std.U8)
  (msg_offset : Std.Usize) (remaining : Std.Usize) (rate : Std.Usize)
  (delim : Std.U8) : Prop :=
  (sponge.pad_last_block.pre message msg_offset remaining rate delim).holds →
  ⦃ ⌜ True ⌝ ⦄
  sponge.pad_last_block message msg_offset remaining rate delim
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_sha3::sponge::absorb_final::pre]:
    Source: 'sha3/src/sponge.rs', lines 77:16-78:95 -/
@[reducible]
def sponge.absorb_final.pre
  (state : Array Std.U64 25#usize) (message : Slice Std.U8)
  (msg_offset : Std.Usize) (remaining : Std.Usize) (rate : Std.Usize)
  (delim : Std.U8) :
  RustM Bool
  := do
  if rate > 0#usize
  then
    if rate <= 200#usize
    then
      let i ← rate % 8#usize
      if i = 0#usize
      then
        if remaining < rate
        then
          let i1 ← core.slice.Slice.len message
          if msg_offset <= i1
          then let i2 ← i1 - msg_offset
               ok (remaining <= i2)
          else ok false
        else ok false
      else ok false
    else ok false
  else ok false

def sponge.absorb_final.spec (state : Array Std.U64 25#usize)
  (message : Slice Std.U8) (msg_offset : Std.Usize) (remaining : Std.Usize)
  (rate : Std.Usize) (delim : Std.U8) : Prop :=
  (sponge.absorb_final.pre state message msg_offset remaining rate delim).holds
  →
  ⦃ ⌜ True ⌝ ⦄
  sponge.absorb_final state message msg_offset remaining rate delim
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_sha3::sponge::absorb_rec::pre]:
    Source: 'sha3/src/sponge.rs', lines 95:16-95:75 -/
@[reducible]
def sponge.absorb_rec.pre
  (state : Array Std.U64 25#usize) (rate : Std.Usize) (delim : Std.U8)
  (message : Slice Std.U8) :
  RustM Bool
  := do
  if rate > 0#usize
  then
    if rate <= 200#usize
    then let i ← rate % 8#usize
         ok (i = 0#usize)
    else ok false
  else ok false

def sponge.absorb_rec.spec (state : Array Std.U64 25#usize) (rate : Std.Usize)
  (delim : Std.U8) (message : Slice Std.U8) : Prop :=
  (sponge.absorb_rec.pre state rate delim message).holds →
  ⦃ ⌜ True ⌝ ⦄
  sponge.absorb_rec state rate delim message
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_sha3::sponge::absorb::pre]:
    Source: 'sha3/src/sponge.rs', lines 113:16-113:75 -/
@[reducible]
def sponge.absorb.pre
  (rate : Std.Usize) (delim : Std.U8) (message : Slice Std.U8) :
  RustM Bool
  := do
  if rate > 0#usize
  then
    if rate <= 200#usize
    then let i ← rate % 8#usize
         ok (i = 0#usize)
    else ok false
  else ok false

def sponge.absorb.spec (rate : Std.Usize) (delim : Std.U8)
  (message : Slice Std.U8) : Prop :=
  (sponge.absorb.pre rate delim message).holds →
  ⦃ ⌜ True ⌝ ⦄
  sponge.absorb rate delim message
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_sha3::sponge::squeeze::pre]:
    Source: 'sha3/src/sponge.rs', lines 141:16-141:108 -/
@[reducible]
def sponge.squeeze.pre
  (OUTPUT_LEN : Std.Usize) (state : Array Std.U64 25#usize) (rate : Std.Usize)
  :
  RustM Bool
  := do
  if rate > 0#usize
  then
    if rate <= 200#usize
    then
      let i ← rate % 8#usize
      if i = 0#usize
      then let i1 ← core.num.Usize.MAX - 200#usize
           ok (OUTPUT_LEN < i1)
      else ok false
    else ok false
  else ok false

def sponge.squeeze.spec (OUTPUT_LEN : Std.Usize)
  (state : Array Std.U64 25#usize) (rate : Std.Usize) : Prop :=
  (sponge.squeeze.pre OUTPUT_LEN state rate).holds →
  ⦃ ⌜ True ⌝ ⦄
  sponge.squeeze OUTPUT_LEN state rate
  ⦃ ⇓ res => ⌜ True ⌝ ⦄


/-- [hacspec_sha3::sponge::keccak::pre]:
    Source: 'sha3/src/sponge.rs', lines 162:16-162:108 -/
@[reducible]
def sponge.keccak.pre
  (OUTPUT_LEN : Std.Usize) (rate : Std.Usize) (delim : Std.U8)
  (message : Slice Std.U8) :
  RustM Bool
  := do
  if rate > 0#usize
  then
    if rate <= 200#usize
    then
      let i ← rate % 8#usize
      if i = 0#usize
      then let i1 ← core.num.Usize.MAX - 200#usize
           ok (OUTPUT_LEN < i1)
      else ok false
    else ok false
  else ok false

def sponge.keccak.spec (OUTPUT_LEN : Std.Usize) (rate : Std.Usize)
  (delim : Std.U8) (message : Slice Std.U8) : Prop :=
  (sponge.keccak.pre OUTPUT_LEN rate delim message).holds →
  ⦃ ⌜ True ⌝ ⦄
  sponge.keccak OUTPUT_LEN rate delim message
  ⦃ ⇓ res => ⌜ True ⌝ ⦄

end hacspec_sha3
